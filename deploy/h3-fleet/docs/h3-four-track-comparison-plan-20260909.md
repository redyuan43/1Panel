# H3 四组优化对比计划

## 最新执行约束：人工效果评审

用户明确要求效果由本人评判。全部候选继续生成和技术验证，不因用户暂时离线而等待，也不由代理代替用户打分、选优或替换默认方案。
已成片的前端卡片统一显示“待人工确认”；技术完整性（真实执行、时长、帧数、音视频解码）与主观效果分开记录。
需要“入围者”才能确定配方的第二 Seed、768p 和12GB适配阶段，保留待人工选择的依赖；当前可以继续完成不依赖选优的环境、调度和独立候选验证，不擅自取消这些阶段或认定验收完成。
最终通知包含全部样片链接、实际配方、GPU、耗时、技术验证结果以及待人工确认事项。

状态：执行中；R0/C0/A4/A8 已生成真实15秒视频并发布，C1 首次推理被资源保护取消，B8/D4 尚未成片；效果均待用户人工确认，未替换生产模型。

## 2026-09-10 内存准入检查点

### 后续真实执行排程

- B8-r2 已实际执行到采样，VDN FP32统计的额外激活申请367.5MiB时显存不足；不是许可、权重校验或排队失败。已确认lease释放和模型卸载。
- C1-r3 使用原配方重新执行，原有冷缓存已通过受控窗口释放；不将首次保护取消当作通过。
- B8-r3 控制器 `h3-comparison-B8-after-C1-20260910.service` 已启动等待C1清理。独立B副本18190、4060Ti、`--reserve-vram 6`，保留8步、362帧、原FP32统计和量化。额外卸载的RAM代价仍由原保护监控。
- D4 控制器 `h3-comparison-D4-after-B8-20260910.service` 已启动等待B8-r3清理。独立D副本18191、4060Ti、隔离Kitchen0.2.33、预留3GiB；原3060 D环境保留用于后续适配。此排程不是三卡并行验证。
- 两个后继控制器必须读到前置任务终态、lease释放、隔离模型卸载证明才启动；仅队列暂空不够。前端同时显示运行与已排程状态，不自动判定效果。

- A8 完整音视频解码通过，执行988.069秒；输出SHA256 `9f0c051d29a85bcef3bb8077d2704710b48e0715d08559db69366da99b84b04e`，已发布对比页。
- C1-r2 真实提交后因Swap增长超过1GiB取消，确认清理与lease释放，无视频产出。不得记为人物LoRA通过。
- 现场Swap约3.4GiB来自zram，磁盘swap使用0；swappiness180、min_free_kbytes2GiB为已有配置，未更改。不能将压缩交换等同磁盘交换，也不能据此忽略PSI或增长保护。
- 旧稳定窗口签名包含换入计数，少量旧页面换入即可反复重置60秒。修复仅排除换入增长；换入计数回退、换出变化、boot/cgroup事件及PSI累计变化仍重置，PSI avg10非零仍阻止准入。
- 已在空队列、持有独占lease时部署上述单文件修复并重启Fleet；五个Comfy生产/实验进程PID不变，随后释放lease。部署SHA256 `54b5f1e25278996ee91809d07d8854bdc1d7f2ddca2f6c64d7c4b46ea550ba28`。证据：Ivan实验目录 `admission-swapin-deployment.json`。
- B8-r2 已准备并启动控制器，等待稳定准入；idle等待上限延长至1800秒，不改变稳定窗口或资源阈值。prompt ID `4e2e1374-2a7c-4545-8919-a52d3b4b3212` 预分配不等于已提交。
- D4 sm86小规模native内核通过，专用3060服务18189在线；尚无完整D4视频。双任务协调器离线测试通过，但仍受Swap≤1GiB硬门限制，未宣称真实并行通过。

## 2026-09-09 执行检查点

- R0：执行556.215秒，任务 `h3val_c7b28eb3c69a4f3f_R0`。
- C0：执行541.772秒，任务 `h3val_c535e13f92034483_C0`；仅增加共同人物触发词，没有启用人物LoRA。
- 两条视频均480×864、362帧、24fps，完整FFmpeg解码通过。抽帧可识别七段广告场景；动作遵循、时间连续性和完整听评不据此判为通过。
- 对比页 `https://ai-x10drg.taild500c8.ts.net:8445/comparison.html` 已通过HTTP200及桌面/手机浏览器检查；视频使用contain，手机无横向溢出。待测项保留占位，不冒充生成成功。
- People原始权重已完成SHA256校验。A/B/D发现固定版本、同大小同SHA256的ModelScope镜像；1MiB直接读取探针为206，约0.43秒，不将小段测速外推为完整下载保证。
- Ivan已有挂载NVMe任务目录 `/media/ivan/44A2C0D9A2C0D11A/h3-comparison-20260909` 的小文件写读/mmap探针通过；尚不等于大权重加载验证，不改变挂载或分区。
- C组发现两个Bypass节点直接串联会覆盖先前注入。改用离线低秩组合，保持单Bypass；真实组合和推理仍须验证，不修改INT8底模。
- 后续顺序：完成校验下载、隔离环境和配方准入；A4/A8/B8/C1/D4单路比较；第二Seed复测基线与入围者；原生768p；入围配方12GB适配及真实三路并发。已完成的R0/C0不重复排队。

### 后续执行检查点

- A4/A8及B组三个权重已在AI缓存和Ivan独立实验盘分别完成全字节SHA256校验；D权重仍下载中，不以文件存在或下载进度判为完成。
- 新增受锁保护的实验盘直连分发器，最多两个文件并行，保存原始来源映射与哈希，正式文件通过同盘rename发布；完整候选预算外保留40GiB，不写入生产models目录。
- 初次隔离复制错误排除了源码内部 `comfy/ldm/models`。已保留失败目录、修复根级资产过滤并补回归测试，重新创建 `h3-comparison-runtime-20260909-r2`，未覆盖生产或失败现场。
- r2 CPU导入：基线T8与B最小插件成功，四个必需节点无缺失。`nodes_replacements.py` 因无运行中的PromptServer出现警告；这不是完整服务启动或推理成功证据。
- 真实底模的250个量化JSON payload已按二进制header长度和实际offset只读提取，用于验证INT8 ConvRot逻辑shape；不凭文件名猜量化格式。
- 当前Kitchen 0.2.31缺少FastH3要求的native Sol VSA接口；D组继续隔离内核/采样审计，禁止用dense回退冒充VSA成绩。

### 首个候选进入真实推理

- AI下载20项全部验证；Ivan六项镜像权重全部验证，People另行传输校验通过。
- C1真实CPU低秩组合完成：1,822,149,460字节，SHA256 `8202f0896559e259846519be6c4703bcae16cb2515f0e8a7cc67aae950a4225f`；systemd记录7.8GiB内存峰值、0 Swap。已发布到实验盘loras，未改INT8底模、尚未验证成片。
- 隔离服务 `h3-comparison-runtime-20260909.service` 在127.0.0.1:18188监听，处于原h3-compute聚合内存cgroup，GPU限定4060Ti；未停止三路生产worker。
- A4前两次尝试均在提交前失败：误用Fleet本地绑定地址、旧验证器不支持已有稳定残留Swap策略。保留失败现场，没有发生重复推理。
- A4第三次通过原有60秒空闲稳定窗口，以1GiB增量Swap门及8GiB绝对硬上限运行，原始资源样本不改写；不是关闭Swap或提高保护阈值。
- 实际提交：`pink20260909-A4-s1-r3`，Comfy prompt ID `0fee52ca-9feb-427c-b5d7-8a32150588f2`，证据目录 `A4-r3`。原fast缓存卸载后该PID仅236MiB显存；隔离任务已获提交回执并进入模型加载，未宣称成片。
- Kitchen 0.2.33完整wheel已取得并通过原始SHA，静态审计有Sol绑定与sm80/sm89等cubin；没有独立sm86条目。是否能在3060上使用sm80代码须实际内核测试，不能仅凭静态清单判通过或不适用。

## 目标与边界

- 四组全部进入比较：LightX2V、OpenVDN、人物写实 LoRA、FastH3/低精度优化。
- 比较画质、提示词遵循、完整成片耗时、RAM/VRAM 和稳定性，不以步数或作者演示速度排名。
- NVFP4 扩散模型必须接受兼容性审计；不支持现有显卡的方案明确列为不适用，不伪造性能对照。
- 维持已经跑通的三路生产预览。候选失败不得污染现有模型、项目、节点目录或调度策略。
- 本次不提交、不推送、不新建分支，不修改驱动、BIOS、Swap 或分区，不调用付费云端生成。

## 主代理与四个子代理

主代理负责清单去重、磁盘预算、测试排队、安全取消、最终复核和生产接入。
子代理不创建下级代理，不互相指挥，不自行启动 GPU 推理或更新共享环境。

| 组 | 当前预检代理 | 负责范围 | 交付 |
| --- | --- | --- | --- |
| A | Hypatia | LightX2V v1.2 768p 四步、v1.0 768p八步 | 两套完整配方、增量文件、版本/参数合同 |
| B | Peirce | OpenVDN DMD8，优先 T8 Comfy 集成 | 分支/adapter 配套关系、最小下载、显存和节点冲突边界 |
| C | Avicenna | fal Realism People；CloseUp 作为备选 | 人物 LoRA 对照、触发词、强度、与 Turbo 的兼容性 |
| D | Dalton | FastH3 VSA 四步、NVFP4 硬件门；W4A8/INT8 VAE 作为备选 | 注意力和权重匹配、可运行/不适用分类 |

每个子代理先只读预检。执行阶段才授予自己的独立任务目录写入权限；
生成由主代理通过带鉴权的受控实验入口统一提交，子代理负责验证工作流和结果。
每组返回来源、固定 revision、文件 SHA-256、命令/工作流、证据路径、状态和首个失败原因。

## 存储与并行下载

现场检查：AI `/home` 约 3.4TiB 可用；Ivan offload 约 45GiB 可用，
现有策略要求保留 40GiB；Ivan 根分区约 28GiB 可用，要求保留 25GiB。
因此不能将四组模型包直接同时下载到 Ivan，更不能将几个文件系统的可用量相加。

1. AI 持久缓存建议设在 `/home/ai/.local/share/h3-comparison-cache/20260909/`。
2. 四个子代理并行准备清单；统一下载器最多两个大文件同时下载，避免重复拉取与争抢网络。
3. 精确 include 所需增量；已有底模、文本编码器和 VAE 先按固定上游 revision 校验再复用。
4. 清单记录仓库、revision、文件、预期字节数、SHA-256、授权状态和本地路径。
5. 临时下载与完成文件分开；校验完成后原子发布。需要访问批准的仓库不得绕过门禁。
6. 传入 Ivan 前计算下载临时文件、安装环境、模型副本、输出和安全预留的峰值空间。
7. 不足时仍可完成 AI 下载和静态审计，但暂停受影响的 GPU 测试。
   需要另行确定可用实验存储；不自动删除旧模型、不扩容 loop、不挂载未知分区。
8. 若使用另一块磁盘存候选，记录模型读取路径和冷热加载差异，不能把 I/O 差异算成算法加速。

## 环境隔离和准入

- 当前基线：ComfyUI 0.34.0 / `250b2e9`，T8 1.3.3 / `5ff46c2`。
- 当前配方：full FL2VA INT8 ConvRot、T8 EMA 四步、dual-clock Euler/native-flow、shift 12/3。
- 现有 LightX2V 四步 v1.0 768p 文件仅说明存在，完成性与正确加载仍须核验。
- A/C 若原环境能够兼容，可使用隔离工作流；B/D 涉及节点或内核变化时，使用独立代码目录和虚拟环境。
- 不让多个代理在同一个 ComfyUI 目录执行更新、安装或下载，不热修改运行中的 Python 模块。
- GPU 实验窗口先确认所有生产/未知任务状态；仅在空闲并取得独占租约时测试。
- 如独立运行时不能与原 worker 共存，先列明仅实验 lane 的停启与恢复步骤；不重启整机或其他模型服务。
- 新候选必须有独立 profile 和模型/节点/adapter/采样配置指纹，不能因同为四步就继承生产三路准入。
- 未知配置 fail closed；只有精确验证过的配方才可以获得相应并发上限。

## 统一输入与公平性

使用 `../../h3-video-studio/docs/validation-prompt-15s.txt` 的粉色广告原始提示词。
保留原文，先按现有流程生成一份结构化 IR 并冻结；所有候选复用其正文和 SHA-256。
必须明确七个镜头的时间分配、同一人物与房间、睡衣到最终时装的换装关系。
电话说话若需要补充具体台词，应在共同 IR 中一次确定，不能各候选自行发挥不同脚本。
人物 LoRA 需要的触发词作为记录在案的唯一提示词差异，开/关对照两侧都保留该词。

- 第一轮固定同一 Seed、15 秒目标、24fps、362 帧、480x864、原生音频。
- 作者工作流若不能支持共同形状或帧数，先查原因；不得静默改成短视频、换比例、删音轨或换任务模式。
- 768p 训练配方在低分辨率下的结果注明分布外使用，不将其等同原生 768p 品质。
- 配方内部遵循作者要求，不能为了相同参数而给不同模型强塞同一 sampler/shift。
- 正式速度/画质排序在同一张 4060 Ti 上按主机串行执行；不同显卡结果分栏，不直接排名。
- 模型加载、conditioning 缓存可单列复用，但每份正式结果必须证明 sampler 真正执行。
- 冷启动、热模型但重新采样、队列等待分别记录，不能混成一个“推理时间”。

## 实验轮次与止损

### 第一轮：七条核心完整视频

| 编号 | 配方 | 主要问题 |
| --- | --- | --- |
| R0 | 当前生产 T8 四步 | 新提示词的速度/画质基线 |
| A4 | LightX2V v1.2 768p 四步 | 同低步数是否改善镜头、细节和声音 |
| A8 | LightX2V v1.0 768p 八步 | 多付出的时间是否换来稳定画质收益 |
| B8 | OpenVDN DMD8 配套完整工作流 | 注意力改造在本机是否改善实际耗时/显存 |
| C0 | 当前 T8 基线 + 人物触发词，不加载人物 LoRA | 排除触发词本身的影响 |
| C1 | 当前 T8 基线 + fal People LoRA | 单独增加人物 LoRA 是否改善人物且不损害分镜 |
| D4 | FastH3 四步 + 匹配 VSA | 是否真正执行新注意力，速度收益与质量损失如何 |

NVFP4 DiT 单独记录硬件预检结果；如果没有适用于现有硬件的有效加速实现，标为不适用，
不强行用慢速仿真跑完整片。当前 NVFP4 AWQ 文本编码器不属于被排除的 DiT 路线。
CloseUp、W4A8、INT8 VAE 不默认展开全排列；仅在主候选失败或出现明确细分瓶颈时补一个受控对照。

C0 与 C1 都带 `r34l1sm`，C1 首轮固定强度1.0，C0不加载人物LoRA；其他组复用不加触发词的R0。
禁止直接用以前 Coser/小火车结果作为新提示词画质基线。不自动裁掉362帧中的两帧，
实际时长仍明确显示15.083333秒；如最终要求精确15.000秒，另作一致的交付裁切，不改原始基准产物。

每项先做静态 shape/key/adapter 合同检查，必要时做分配和短前向兼容探针；探针不是5秒成片验收。
正式验收全部用15秒。分配失败只允许一次有明确因果依据的兼容性修正，禁止无限重试。
预览首轮默认单条30分钟上限；达到上限保存实际进度并记为预算中止，不能当 OOM 或已完成结果。
单独记录一次性编译成本；需要更长预算才能完成的方案进入慢路径候选，而不是卡住整个比较。

### 第二轮：只复测入围者

- 基线加最佳两个候选使用第二个 Seed，各补一条15秒，排除一次抽样运气。
- 对质量/速度最有价值的一至两个方案做原生768p、15秒确认，独立设置时间和内存预算。
- 不再逐卡跑同样的15秒阶梯；只有需要推广到12GB卡的入围方案才做对应硬件适配。
- 不把尚未验证的14步高清全流程作为第一轮比较的前置条件。

### 第三轮：调度与交付

- 先完成入围配方的12GB适配与单路内存合同，再运行三个独立15秒任务的真实并发验收。
- 新配方的三路并发和“高清+低清”属于两个独立能力；不能拿一个的结果开放另一个。
- 页面提供基线/候选对照、实际规格、耗时和失败状态；候选默认不覆盖已生成的视频。
- 优先交付可观看对照，再决定快速预览、质量增强、实验功能各采用哪个配方。

## 观测、验收与结果

复用现有监控，至少记录 RAM MemAvailable、cgroup 当前/峰值与 OOM 事件、
Swap 增长和换入换出、PSI、主机 IMC 带宽、GPU VRAM、内存控制器活动、PCIe 收发、温度和降频。
主机带宽与 GPU 显存带宽不得混淆；采样峰值不是硬件带宽上限。
沿用现有16GiB RAM下限、72GiB cgroup上限、1GiB Swap门、根盘25GiB/offload40GiB预留、90C止损。
OOM/Xid、监控失联、未知任务或运行时变化触发仅取消自有任务，保留日志与已完成产物。

完整成片要求 MP4 全解码通过、实际帧数/时长正确、音轨存在且检查无明显异常，
桌面/手机完整画幅可见、历史项目切换对应正确视频、页面显示真实耗时。
视频画质分别评价七镜头覆盖、人脸/手部、镜中一致性、服装变化、粉色光影、闪烁和音画同步。
匿名 A/B 人工观感与机械检查分开；不拿 SSIM/清晰度分数替代审美或剧情理解。

每项最终状态必须是：完整通过、画质不合格、性能不划算、硬件不适用、依赖/存储阻塞、
预算中止或运行失败之一。没有视频的项目也必须给出原因和证据，不能从表格中消失。

通过评估不自动替换生产。主代理在本次已授权边界内安排可回滚交付；
涉及磁盘清理、分区、驱动等额外风险时，单独说明并取得确认。

## 已复核的子代理预检补充

C1仅需 `h3-realism-people-t2v-i2v-r2v.safetensors`，固定权重提交
`b6e6b9f683c80c38684dbb1adcd566fc69a711fb`，131,229,656字节，SHA-256
`acc529601d2da117fb81179e76c56e488a3beab1171659d305f04fa3655b787e`。
作者说明该LoRA通常面向FP16/FP8，因此本次INT8 ConvRot + T8 EMA组合必须单独验证；
失败不代表LoRA在作者原始配置下无效，也不能为跑通而静默更换大底模。
来源：[固定权重提交](https://huggingface.co/fal/MiniMax-H3-Realism-People-LoRA/commit/b6e6b9f683c80c38684dbb1adcd566fc69a711fb)、
[作者兼容性回复](https://huggingface.co/fal/MiniMax-H3-Realism-People-LoRA/discussions/4)。

### A/B/D 子代理候选清单

以下 revision 与大小为子代理预检返回的下载计划输入；实际执行前，主下载器仍须从
固定 revision 取元数据并逐文件核对哈希，不能仅凭表格视为已下载或本机已验证。

| 组 | 最小增量 | 固定候选 revision | 说明 |
| --- | ---: | --- | --- |
| A | 已有A4校验一致时约1.822GiB，否则约3.644GiB | `lightx2v/Minimax-h3-Turbo@2f015e66b37c585cea9dc4ae6f1850ea8788e742` | 四步与八步768p Comfy BF16 LoRA各约1.956GB |
| B | 约5.090GiB，另加小型配置/许可文件 | `t8star/Vdn-Minimax-H3-Comfy@d6aa5dfc669a749dc9364a36f3b6d4d1545a53e8` | 只下载DMD250线性分支、default与turbo adapter，不下整包 |
| D4 | 约21.326GiB | `Kijai/MiniMax-H3-experimental@f4cac997f880e93cf6940af61ee8d58ef31ff7f3` | `minimax_h3_fastvideo_vsa_datafree_1300step_4step_int8_convrot.safetensors` |
| D备选 | W4A8约11.680GiB；INT8 VAE约2.954GiB | 同上 | 首轮不额外展开或下载 |

A组配方单独核验shift6/3，移除T8 EMA；共同480p用于产品预览比较，原生768p进入第二轮，
不采纳以横版1344x768替换共同竖版输入的建议。B组候选节点提交
`e4ff8607a982ecea22d68347efc4bcc356b996ea`仅在隔离环境审计；不得为兼容而绕过结构/哈希校验。
FastH3必须验证VSA gate张量被完整加载并实际调用对应内核，不能加载成功却退回dense还记作VSA成绩。
NVFP4 DiT记录为本硬件不适用的原生加速路线，计划权重下载量为0；并不据此删去D组FastH3对比。

来源：[LightX2V规格](https://github.com/ModelTC/Minimax-H3-Turbo)、
[VDN清单](https://huggingface.co/t8star/Vdn-Minimax-H3-Comfy)、
[Kijai模型](https://huggingface.co/Kijai/MiniMax-H3-experimental)、
[FastH3任务和内核边界](https://huggingface.co/FastVideo/FastVideo-FastH3-4-step-Preview-v1-VSA-DataFree)。

主代理随后通过固定revision的HF API复核A组字节数，并直接读取B组固定manifest，
确认上述A/B大小。另发现同一LightX2V revision还包含四步v1.1和v1.2，原先README规格表未列出。
因此A4版本选择增加一道门：先核对新版作者采样合同与发布说明；确认适配后优先比较最新候选，
v1.0保留为已有回退，不盲目展开三个版本的全排列。未知合同不能直接继承v1.0的6/3配置。
v1.2文件为 `minimax_h3_fl2v_turbo_4step_v1.2_768p_comfyui_bf16.safetensors`，
1,956,193,000字节，SHA-256 `c8168ebc17bbacc4296103dda2fec1ba85b24392fa08cf2bfbcef0cff0dc3cc8`。
若选择v1.2替代已有v1.0，A组需新增两份LoRA约3.644GiB，而非仅八步的1.822GiB。

最终A组选择：A4改为v1.2，A8保留v1.0 768p；不额外下载v1.1。
子代理补核了[作者v1.2发布说明](https://huggingface.co/lightx2v/Minimax-h3-Turbo/discussions/52)：
四步、video/audio shift6/3、Euler，作者报告相较v1.1改善音频纯净度、稳定性与伪影。
发布正文未完整指定scheduler，不能把社区回复中的simple当作者合同；
适配时仍须核对作者执行图/代码的实际时间步和音视频联合调用，T8集成未验证前不得提交正式比较。
