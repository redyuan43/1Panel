# 生图策略验证与发布记录（2026-09-24）

## 范围与版本

- 在独立 `codex/image-policy-20260924` worktree 完成实现、审查和验证。
- 发布前提交为 `4b6329e43`、`cde8faa67`、`e73450787`；准备 PR 时仅把这三项
  生图改动重放到 `fork/dev-v2`，没有带入本地未发布的视频和其他工作区修改。
- 整合后的生图提交为 `c2496d9a0`、`24b89e585`、`1e75c420b`；range-diff
  确认实现未改变，仅状态文档的新旧章节衔接不同。
- 20:51 +08:00 的发布回读：两台 API 的后端补丁为 `cde8faa67`，两台管理服务
  含 `e73450787` 的界面补修；源码哈希匹配、健康、零重启，API 未处于排空状态。
  Git 整合不等于重新部署整个远端分支。

## 实现与审查

- 图片拥有独立版本策略：草稿、验证、激活、回滚。每个任务保存策略快照。
- 本地优先；忙时按全局许可、账号授权及额度选择云端；明确模型/通道不得静默替换。
- 未知结果只查询原任务，保留资源；明确额度拒绝可以回本地排队，不反复尝试云端。
- WorkBuddy 客户端保存原回执，恢复轮询和下载；重复下载复用哈希匹配的文件。
- 修复公开模型列表的位置切片、云端图片方向校验、额度拒绝后排队，以及文本简洁
  模式隐藏生图入口的问题。公开列表保留图片与视频稳定别名。

## 隔离测试

- 第一阶段目标回归：367 项通过，91.76 秒。
- 最终额度恢复补修后的目标回归：81 项通过，8.98 秒；与上一组重叠，不相加。
- 在实际生产基线叠加补丁的候选上：90 项通过，9.73 秒。
- 真实样式浏览器检查覆盖简洁模式、流程卡片、配置采集、资源显示和版本操作。
- 边界包含全局/账号禁止云端、明确通道冲突、共享额度、队列期限、策略冻结、
  丢回执与恢复、取消竞争、未知结果不重发、损坏输出、跨账号访问和下载恢复。
- 故障注入采用隔离模拟后端；没有中断生产显卡或运行视频模型来制造故障。
- PR 整合基线为 `a9e20dc57`；整合后 369 项回归通过（89.09 秒），真实样式浏览器检查通过。

## 真实链路

请求从 ivan-laptop 已安装的 WorkBuddy Skill 客户端发出，使用原有本机凭证。
下表耗时为接单至归档，含排队，不包含调用轮询间隔后的人工等待和客户端下载。
本轮只验证功能，不评价画质。

| 路径 | 任务号 | 耗时/秒 | 文件尺寸 | 结果 |
|---|---|---:|---|---|
| NX5 本地生成 | img_2a13864200d44617b5af695d9adf297a | 128.12 | 1280×720 | 完成、下载、哈希匹配 |
| 本地忙时转云端 | img_30588219b47143fe8151506908fa2f0a | 57.60 | 1672×941 | 完成、下载、哈希匹配 |
| 指定云端生成 | img_0c3462c534c9485d96f428d032df02c2 | 26.16 | 1672×941 | 完成、下载、哈希匹配 |
| NX5 本地编辑 | img_e0614b2b7ac645e58877b0aa48894419 | 559.16 | 1376×768 | 完成、下载、哈希匹配 |
| 云端编辑 | img_8c19298d00194344a34049ce60c1a7b8 | 37.13 | 1672×941 | 完成、下载、哈希匹配 |

- 5 个任务均使用策略修订 23；服务端与客户端实际文件的大小、SHA256 独立核对一致。
- NX5 上线前另有 1280×720 验收：Qwen Image 2.1、20 步、CFG 1.0、euler/simple，
  ComfyUI 实际执行 329.951 秒；未降低生成尺寸。
- 自动分流审计为 `local_busy_cloud`；本地执行为 `local_available`。
- 云端结果重复下载返回 `reused=true`，没有新增生成任务。
- 生产浏览器可显示资源状态、任务事件、耗时、下载回执；最后回读 NX5 空闲、
  在途任务为 0，浏览器脚本错误为 0。
- 前后 17 条视频任务记录一致；视频模型和 GPU 服务没有在本轮验收中运行或替换。

## 已开放和未验证

- 已开放：NX5 本地一路、Codex 云端一路。云端付费备用仍关闭。
- NX6 未交接，保持禁用；NX7 不纳入本次资源池。不能声称双本地节点上线。
- WorkBuddy Skill 客户端、API、轮询、下载已验收；桌面对话自动触发 Skill 并展示
  链接尚未做 GUI 层验收。人工画质和内置 ImageGen 效果对照未做。
- NX5 本地编辑较慢；执行阶段温度采样最高 98.968℃，不能因此断定温度是耗时原因。
  没有调整功率/散热，本次成功不等同持续高负载稳定性验收通过。

## 工件与回滚

详细私有证据保留于任务 worktree 的 `outputs/image-policy-release-20260924/`：
Markdown 报告、精确基础镜像、窄补丁哈希、回滚清单、任务与客户端下载证据、截图。
这些诊断和含私有配置的发布工件不进入 Git/PR。

回滚前先核对运行版本与活动任务，保留持久任务状态；未知任务不得重新生成。
原媒体 release、基础镜像和 WorkBuddy 文件备份均保留。
