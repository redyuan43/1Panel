# 上下文压缩与历史找回交付记录

最新提交验收与未完成边界见 [2026-09-14 验收摘要](context-memory-acceptance-20260914.md)。本文保留各阶段原始状态，不将早期记录当作最终结果。

## 目标和开发基线

- 工作分支：`codex/context-memory-20260913`。
- PR 目标：`redyuan43/1Panel:codex/ai-router-media-h3-20260905`。
- 起点：`4ec6d3193d8fe02845ae7be3ed66bef118b19d45`。
- 主工作目录保持不动；只复制已有上下文策略、分段压缩、Codex 健康检查与上下文元数据、DeepSeek 工具历史兼容性以及对应测试/界面的必要基线。未复制 GLM 注册表调优、诊断脚本、H3 和其他未跟踪文件。
- 验证通过后才提交、推送、创建 PR；合并与上线分别记录，不把代码提交当作生产验证。

## 必须交付的行为

1. 保留原上下文管理、原消息/胶囊兼容及压缩/扩大上下文两条策略。
2. 按模型窗口分配摘要输入，按消息/工具事务/段落分段；结构化交接摘要、原文来源、最近历史保护、完整性/长度检查。
3. 加密持久化后台压缩任务，去重、取消、重启恢复、预算约束、源历史验证后再应用。上游结果未知不盲目重发。
4. 用户选择：允许已接入云模型，质量优先且有上限，支持后台预压缩。
5. 用户最新确认：所有 Router 客户端账号支持找回本账号的历史，不要求自然人独占声明。隔离边界是已认证的 Router client_id；同账号多密钥共享本账号历史，不读取其他账号，不把 WorkBuddy 用户 ID 当认证依据。云端及旧未知来源的授权仍单独控制。
6. 复用清理暗号后的加密归档，独立可重建的加密内容/HMAC 检索词索引；不索引系统提示、隐藏推理、凭据及自动补充内容，不新增明文全文库。
7. 本地查找路径/错误编号/中英文关键词；可靠匹配不足时至多一次受控查询改写。补充片段带来源与时间，不能覆盖当前指令或提供执行授权，也不再次进入原始会话/索引。
8. 回查最多 6 段、4096 token、目标窗口 10%，最多 5 秒；查询改写最多 2048 输入/256 输出 token，异常可观测地跳过回查。
9. 压缩默认 80% 触发、目标约 60%，摘要单次生成额度默认 8192 token，可配置但仍受模型与整任务预算约束；后台总计最多 600 秒、32 次调用、100 万输入/6.4 万输出 token，全局并发 1。
10. 管理界面：账号授权、会话排除、索引进度、原文定位、压缩任务创建/查询/取消、预算与模型设置、失败原因。激活前和调用前分别验证依赖。
11. 每次回查执行账号/来源/目的模型权限检查、重新计 token、保留路由硬约束；不自动开启 GPU 后端或更换供应商。

## 验收门槛

- 单元/全量回归/界面、旧胶囊、Chat/Responses、暗号、会话分支兼容。
- 33.9 万 token 长输入、超大工具输出、连续五轮压缩、跨会话丢失细节找回、中文改述、无匹配/纠正场景。
- 测试集关键约束和工具配对全部保留；事实问答准确率 >=95%；越权引用为零。有限测试集不代表永不遗忘。
- 429/503、截断/空输出、取消、并发竞争、进程重启、来源变更、撤销权限、敏感会话排除、索引损坏/缺失。
- 隔离测试环境使用独立端口、Redis、数据库和归档；生产变更仅涉及本次需要的 Router 组件，排空活动请求后滚动替换，保留回退镜像。
- 重启后配置参数并完成真实模型/真实协议验证，记录模型版本、请求 ID、源文件哈希、质量/耗时/token/费用证据；通过后才能创建 PR。

## 当前证据

- 已创建独立 worktree，尚无本功能提交、PR 或部署。
- 后续测试与运行证据在本节追加，不使用计划代替结果。
- 2026-09-13：独立 worktree 复跑 `test_context_policy.py`、`test_codex_adapter.py`、`test_content_audit_contract.py`、`test_memory_index.py`，70 项通过。
- 加密索引/原文提取已实现独立模块；尚未接入线上请求。覆盖账号隔离、密文串换检测、同历史去重、来源限制不可放宽、中文/标识符检索、排除会话、敏感文本过滤。
- 账号新增个人归属确认、历史找回、云端历史授权，默认全部关闭；严格校验布尔类型，撤销上级授权同时清除下级授权，恢复上级开关不隐式恢复云授权。调用前重新读取账号和密钥状态的接口已实现，尚待回查链路调用。
- `python3 -m pytest tests/test_memory_grants.py tests/test_memory_index.py -q`：48 项通过；`python3 -m pytest tests/test_core.py -k "client" tests/test_media_service.py -q`：14 项通过（该过滤也作用于 media 测试，不代表完整 media 回归）。`git diff --check` 通过。
- 未完成：归档历史权限快照/回填、请求侧临时回查与预算、查询改写、后台压缩任务、摘要质量增强、控制界面、全量回归、真实协议及模型验证、部署重启和 PR。当前测试不证明这些功能已经交付。

### 归档权限与增量事件

- 请求归档已接入原始 local-only 权限快照，取账号与全局路由约束的合并结果；索引提取不能以今天的云授权覆盖原本 local-only 的来源。旧记录未知权限默认不出云，必须另外明确批准旧记录的云端使用；未知版本/非法权限值保持禁止。
- 归档新增仅含请求 HMAC 的递增事件表及数据库触发器。首次初始化为既有记录补事件；后续写入同事务追加事件，包含旧版本进程滚动运行时的写入。没有另存明文消息，原有归档字段未删除。
- 只读分页接口按账号过滤并核验请求 HMAC 与解密载荷一致；以事件序号推进而非请求创建时间，避免漏掉迟完成的响应。此接口尚未接入后台索引 worker。
- `test_memory_archive_events.py`、`test_memory_index.py`、`test_memory_grants.py`、`test_content_audit_contract.py`：80 项通过，覆盖晚完成、账号过滤空页推进、重开不重复补事件、旧进程写入触发事件、密文串换、权限快照。
- 扩大过滤回归发现 `test_ai_image_is_resized_for_inference_and_original_is_archived` 失败：预期 1024×512、实际 2048×1024。在原工作区同一测试复跑亦同样失败，已确认是已有基线问题，尚未修复；不能将该轮回归报告为全通过。
- 生产尚未应用数据库增量结构；正式部署前必须说明新增事件表/触发器的空间及写入开销、先备份并在隔离环境验证迁移与回退兼容。

### 后台索引接入

- API runtime 已接入后台归档索引 worker；没有获授权账号时不创建索引文件，不启动模型调用。每页重新检查账号归属/授权，共享 worker 锁避免多实例正常情况下重复处理。
- 持久游标只在原文写入成功后推进；写入成功而游标未提交的中断可重放，靠来源标识去重。关闭时等待已开始的 SQLite 线程结束，再释放 worker 锁。
- 集成测试发现并修复归档 JSON 编码包装导致助手回答漏提取的问题。
- 归档/权限/索引/内容契约组合回归 84 项通过；随后新增取消期间等待写入、读归档期间撤销授权两项，`test_memory_service.py` 共 6 项通过。
- 后台索引现已接入 runtime；请求侧回查、旧未知权限记录的额外云授权设置、索引状态管理界面、摘要和压缩任务、真实服务验证仍未完成。
- worker 锁当前使用 120 秒租约；长页处理与租约续期/失效的专项验证仍待完成，不把普通锁竞争测试当作无限时长互斥证明。
- `test_core.py -k "startup or shutdown or restart or streaming_response_is_written_to_training_archive"`：4 通过、1 失败；失败项 `test_ai_pool_marks_restarted_worker_cache_generation` 预期 `worker-priority-1`、实际 `ai-qwen38-27b`，原工作区同项复跑得到相同失败。属于待处理的已有基线问题，不能算整轮回归通过。

### 请求侧临时找回

- 已接入选定模型之后、云端预算预留之前的找回步骤；最多 6 段、4096 token、窗口 10%，重新计数并检查输出预留。增加账号 TPM 用量，不重复计 RPM。无权限/无匹配/空间不足/超时/索引故障则记录原因并跳过。
- 原 `routed_body` 不变；临时副本放在仅内部使用的 decision 字段中，只有 `_send_upstream` 发送时使用。发送前复核账号/密钥、原请求指纹及来源是否仍可读取；变更或撤销时丢弃临时副本，不修改原会话。预算已预留后跳过找回可能保守多计估算 token，尚需联调核对最终实际用量和重试时追加计数。
- 136 项上下文/适配器/归档/权限/索引/找回组合测试通过。补充了真实 `_send_upstream` 函数的 MockTransport 测试，覆盖 Chat、原生 Responses、Responses 转 Chat；不等于真实模型调用。
- 请求侧尚缺受控查询改写、完整历史持久化/分支/SSE 联调和实际模型的质量验收；当前关键词找回不能证明中文改述或丢失细节召回质量达标。

### 压缩器分段与输出校验

- 摘要输入窗口改为注册模型安全窗口减去 8192 输出预留，不再固定封顶 32768；能放下时只调用一次。按完整 Chat/Responses 工具事务合批，仅单个事务本身过大才拆片，拆片带源 SHA256 和连续字符位置。
- 分段共享 32 次调用、100 万累计输入、6.4 万累计输出及 600 秒工作预算检查，并检测合并不收敛。前台请求仍受现有 180 秒外层超时保护；持久化后台压缩任务尚未实现，不能据此声称已支持后台恢复。
- 摘要输出上限改为 8192；拒绝截断/过滤/工具调用结束、JSON 错误、非法字段类型、非空历史的全空摘要；拒绝没有缩短历史的胶囊。压缩后目标从 40% 调整为 60%，系统消息和最近完整事务仍保留。
- `test_compaction_quality.py`、`test_context_policy.py`、`test_tool_schema_compat.py`、`test_intelligent_v2.py`：169 项通过；随后增加 Responses 事务分组用例，单独质量契约测试共 11 项通过。现有预算测试同步改为检查模型窗口减输出预留，不再断言旧固定值。
- 当前测试证明分段/预算/失败保护，不证明摘要语义质量达到 95% 事实准确率；源引用的归档定位联调、五轮压缩、Pro/Flash 实测及后台任务恢复仍待完成。

### 后台压缩任务存储

- 已实现独立加密 SQLite 任务/步骤检查点存储；按账号、分支、原始请求和参数去重。账号和去重索引使用 HMAC，消息/候选/步骤结果只存密文。
- 领取任务用事务与租约隔离；写入 dispatch 意图后没有保存结果的步骤不自动重发，重启后转 `needs_context` 并阻止继续领取；已完成步骤按输入指纹复用结果而不重复累计调用。
- 候选应用必须由当前请求发起，校验账号、分支、协议及完整原始消息前缀，保留后续追加消息。不从后台直接更新会话，取消操作隔离迟到的 worker 写入。
- `test_compaction_jobs.py` 与 `test_compaction_quality.py`：18 项通过，覆盖双实例竞争、重启检查点、未知结果、取消、预算、原文变更及明文不落库。
- 仍待完成：任务执行器接入摘要调用、上游 operation ID 对账、取消在途调用与资源释放、600 秒跨重启预算、租约续期、候选自动应用、管理 API/UI。当前仅证明任务存储合同，不是后台真实运行验收。

### 摘要调用检查点接入

- 新增每任务独立的 `CheckpointedCompactor`，分段/合并摘要均可复用此实现。按完整摘要请求和目标地址计算步骤指纹；发请求前持久化 dispatch 意图，并将保存的 operation ID 带到实际 HTTP 请求头。API key 不进入步骤指纹或持久化字段。
- 通过原摘要函数完成 HTTP 与结构校验后才保存步骤结果；重启复用已保存结果，超时/未保存结果不重发。发送前再次验证任务租约与取消状态。
- 增加累计运行时间记账；心跳/步骤写入保存已用时间，过期恢复按租约上界计入未记账区间，不把排队和离线等待重算为新的预算。跨重启后累计达到 600 秒拒绝新调用和候选发布。
- 取消尚有在途未知结果的任务进入 `needs_context`，不会释放全局任务执行资格去启动下一个任务；仍需要上游对账/恢复管理入口。
- `test_compaction_jobs.py`、`test_compaction_checkpoint.py`、`test_compaction_quality.py`、`test_context_policy.py`：49 项通过。覆盖实际摘要函数 MockTransport 单次调用复用、operation ID、超时不重发、跨重启时间预算以及在途取消。
- 尚未接通 runtime 后台执行循环、受控模型准入/预算、结果核对接口及设置 UI；没有生产后台任务或真实模型验证证据。

### 后台执行循环与 Router 准入

- API runtime 已接入后台压缩 worker，配置 `compaction.background_enabled` 默认 false，严格布尔校验；关闭时不创建任务数据库或发送模型请求。
- 任务领取后按剩余累计 600 秒运行并定期续租；成功只发布候选，异常记录失败/未知结果，关闭服务时取消并等待执行任务清理。
- 摘要调用经过当前账号/密钥授权、Router 明确模型选择及本地限制、容量获取、账号并发/RPM/TPM、云预算预留。结束时释放容量和账号并发，未知结果保守消耗预算预留。禁止后台静默更换已配置摘要模型。
- `test_compaction_jobs.py`、`test_compaction_checkpoint.py`、`test_context_policy.py`、`test_memory_grants.py`：68 项通过；后台执行器测试 7 项通过，覆盖默认关闭、候选发布、失败退出、关闭取消、模型配置变化以及调用成功/失败的容量释放。
- 执行器目前未有管理入口或自动提交触发。仍需端到端故障对账、实际摘要 token/费用核算、复用缓存步骤不重复占用 RPM/TPM、配置/管理 API/UI、请求候选应用及真实生产验证。模拟执行器/准入测试不能代替实际 Router+模型联调。

### 历史找回控制台入口

- 账号编辑界面已接个人独占确认、跨会话找回、云端历史授权；撤销父权限清空依赖授权，重开不会自动恢复云授权。本地限定账号不能选择云授权。设置界面新增默认关闭的后台预压缩开关。
- 新增管理员索引状态、会话排除/恢复、来源原文查看接口；原文查看额外校验账号当前授权与来源排除状态，并写审计。界面用 textContent 展示原文，避免执行原文中的 HTML。
- 索引改存于 `settings.runtime_path` 同目录的 `history-memory.sqlite3`，与 Router 状态保持一致，控制台无需写原始归档目录。GET 使用只读 SQLite 连接；缺失索引时只返回未初始化，不创建数据库。未上线过的功能无生产索引迁移操作。
- 索引/后台索引/授权回归 64 项通过；新增控制台接口测试通过，覆盖无管理员认证、缺失索引不落盘、跨账号拒绝、排除后原文不可读、严格布尔参数及原文访问审计。JavaScript 语法检查及实际表单收集/授权联动测试通过。
- 尚未完成浏览器真实渲染验收、压缩任务管理与恢复入口、自动提交/候选应用、旧未知权限历史的单独云授权；目前界面不能被当作完整功能交付。

### 自动预压缩及候选应用

- 获授权请求在已选模型上下文占用达到 80% 后自动去重提交任务；仅在允许压缩且后台开关启用时触发。扩展上下文模式沿用现有禁止主动压缩逻辑，不调用提交/应用路径。
- 下一次请求在前台压缩预检前尝试候选，验证当前账号授权、来源密钥仍有效、摘要模型未变化、分支和完整历史前缀匹配；保留新增尾部和当前工具字段。候选必须缩短请求才应用，原入站消息不修改。应用任务 ID 写入路由追踪。
- 后台缓存步骤在模型容量取得后、账号 RPM/TPM 和云端预算预留前复用，避免恢复时重复占用用量预算；容量仍按正常路径释放。
- 自动触发/上下文策略/智能路由/执行器组合 66 项通过；缓存预算保护补测后，执行器/自动触发/任务存储/检查点组合 22 项通过。含阈值、去重、新增尾部、旧消息改写、撤销密钥与权限、缓存无新调用/无重复 RPM/TPM。
- 尚无真正完成的后台模型任务或服务重启验收。任务管理/结果对账入口、浏览器验证、实际超长会话与五轮压缩、受控查询改写、旧未知来源云授权及完整回归仍待完成。

### 压缩任务管理

- 管理员可按账号列出任务摘要、基于归档请求 ID 创建任务、取消任务。创建不接受任意历史正文；核对路由追踪及归档所属账号、真实分支、原密钥仍有效、目标与摘要模型已启用、摘要窗口足够及来源本地限制。
- 任务列表仅展示 ID/分支/状态/次数/token/时间/错误，不返回原始请求、摘要正文或步骤结果。GET 使用只读连接，任务库不存在时返回空列表而不创建文件。
- 账号界面已接刷新、按归档创建、按任务 ID 取消，明确创建会调用模型、在途取消可能仍需对账。
- 任务存储/检查点/历史控制接口组合 12 项通过；新增压缩管理接口测试通过（管理员认证、归档归属、正文不泄露、跨账号取消拒绝、本地来源云端拒绝、正常取消）。JS 语法与表单回归通过。
- 已启动完整回归 `python3 -m pytest -q --tb=short`；尚未得到终态结果，不能报告全量通过。

### 完整回归与浏览器第一轮

- 独立工作区和原工作区两轮 `python3 -m pytest -q --tb=short` 均已结束，均未全绿。独立工作区初次 41 个失败；其中 6 项为本次发送函数直接读取可选 `recall_projection` 引发，已改成缺省兼容读取。
- 修复后 `test_endpoint_timeout.py`、`test_prompt_directive_additions.py`、`test_memory_recall.py` 30 项通过；剩余 35 个失败与原工作区本次完整回归的有效失败集合一致。原工作区当前实际收集 1658 项；比较时按当前收集集合过滤了缓存中的 20 条已删除/改名旧失败记录，不能直接用 lastfailed 文件总数当本轮失败数。
- 本次尚未在修复后重跑完整套件，只有专项复跑证明 6 项新增失败已修复；其余已有 35 项仍需归因及明确验收处理，不能称为全量通过。
- 新增 `tests/browser_context_memory.cjs`，仅允许 127.0.0.1 且要求隔离预览标记，阻断其他 origin。桌面 1440×1000 和手机 390×844 的授权依赖、撤销后不隐式恢复云授权、索引/任务状态查询通过，无页面脚本异常；已查看手机截图确认新增控件可读可操作。
- 浏览器测试使用临时目录 `/tmp/1panel-context-browser.dgZ2CR` 下的 playwright-core 和已安装的 Chromium 缓存；没有修改仓库依赖或安装系统包。系统 `/usr/bin/chromium-browser` 是缺少 snap 的包装器，首次未能启动，改用已有缓存成功。截图在该临时目录 `artifacts/`，属于临时测试证据。
- 隔离预览服务 127.0.0.1:14871 已停止，退出码 143；生产服务未操作。任务恢复核对、查询改写、旧未知来源云授权、真正长对话质量测试与部署验收仍未完成。

### 已知失败恢复与启用检查

- 已区分收到 HTTP 响应的失败与读取/传输结果未知：后台对明确 429/503 最多自动重试一次，生成新 operation ID 并保存前次状态；已完成片段通过检查点复用。读取超时仍不重发。
- 只接受有限且不超过 60 秒的数字 Retry-After 自动等待；日期格式、非有限值、负值或更长等待不擅自提前重试。已返回的无效/空/截断摘要按已知失败记录，不再误归类为未知结果。
- 累计次数与输入预算包括重试；失败响应缺少可信输出用量时保守占用输出预留。失败任务保留首个失败状态和 operation ID，成功候选清除当前错误但保留失败历史。
- 后台开关启用前检查摘要模型已注册/启用、窗口充足，以及云模型/供应商授权和预算；直接设置写入、草稿更新和真正激活时均检查。此处是配置依赖校验，实时健康/容量仍由每次调用的 Router 准入验证。
- 错误恢复/压缩器/任务/执行器组合 46 项通过；控制入口/上下文/执行器组合 41 项通过。含 429/503 首次失败后成功、第二次失败停止、已知坏摘要不重试、未知超时不重发及启用依赖拒绝。
- 未知上游结果的人工/自动对账入口仍待实现；不能将已知 HTTP 失败的一次重试当作全部故障恢复已完成。

### 未知结果的人工核对入口

- 管理员必须填写未决 operation ID、核对证据引用，并明确确认上游已经结束及放弃结果，才能把对应未知步骤标记为已放弃。此处是管理员声明，不是系统已经自动向供应商验证任务终止。
- 核对严格绑定账号、任务及未决步骤；不会重发原请求、伪造摘要或删除原始请求。仅在没有剩余未知步骤时结束任务并允许领取其他任务。原任务去重仍有效，不能借此自动重做原调用。
- 证据引用只存加密任务正文，不出现在任务列表或接口响应中；审计记录操作类型、任务、账号、operation ID 和请求来源。界面展示未决 ID，提供明确确认入口。
- 任务存储、管理接口、执行器和摘要检查点组合 37 项通过；JavaScript 语法、上下文设置与授权联动测试、git diff --check 通过。没有生产调用或重启；自动供应商对账仍未实现。
- PR 继续以完整验收为前提，目标为 codex/ai-router-media-h3-20260905；尚未提交、推送、创建 PR 或合并。

### 历史索引租约续期

- 索引页处理期间每 20 秒续租 120 秒工作锁；Redis 通过 Lua 原子比较持有者再续期，内存存储实现相同的持有者与过期检查。旧实例不能续租或释放新实例的锁。
- 续租失败或连接异常后停止后续分块提取，写入前及游标推进前重新检查持有权；提取中止会使当前 SQLite 添加事务回滚。已经完成的添加但未推进游标仍按既有去重机制安全重放。
- 取消时继续等待在途 SQLite 线程完成，其间续租仍运行，之后才停止心跳及释放锁。Redis 租约和 SQLite 不是同一事务，不能声称这消除了机器暂停/网络分区下所有短暂重叠；数据库事务、去重及单调游标仍是恢复保护。
- 索引服务/索引存储/后台压缩执行器/检查点组合 65 项通过，包括慢写入取消期间续租、持有者被替换、续租连接异常、过期锁不能复活。此次未连接生产 Redis；Redis Lua 分支仍需隔离实例实测。

### 受控查询改写

- 新增 compaction.history_query_rewrite_enabled 设置及界面开关，默认关闭。沿用明确配置的摘要模型；启用检查沿用已注册/启用、上下文和云授权依赖，不自动更换模型。无需开启后台压缩才能单独开启查询改写。
- 仅在原关键词无可信结果后调用一次，仅发送脱敏当前问题，不携带历史、工具或会话。调用经过当前历史权限/密钥、Router 明确模型匹配、本地限制、容量、账号并发和 RPM/TPM、云预算；发送前再次检查授权和全局本地限制。
- 输入请求最多 2048 token，输出预留 256 token；最多使用总找回预算中的 3 秒，并给后续检索预留时间。输出必须是完整 JSON 的非空 query 字符串，拒绝截断/工具调用/超长；不重试 HTTP 错误或未知超时，已发送调用保守计入预算并释放容量和账号并发。
- 改写后仅再查一次索引，仍受原来的账号、来源云限制、排除、可信匹配门槛及投影窗口限制；无匹配则可观测跳过。查询和返回原文不进入改写审计，仅记录 operation ID、账号、结果类别。
- 查询改写/历史投影/后台执行器/上下文策略组合 74 项通过；设置表单与 JS 语法、git diff --check 通过。覆盖单次调用、直接命中不改写、改写后无结果不循环、敏感字段脱敏、503/超时/坏 JSON/截断/工具输出、权限和预算准入。模型响应使用 MockTransport，中文同义改写的真实质量和实际费用尚未验证。

### 最近巨型工具结果压缩

- 确认旧分区至少保留四条消息，使最近的巨型工具返回即使经过旧历史摘要仍超限。新增仅在候选超过 60% 目标时的工具结果文字压缩；不截断用户/系统指令、不改变工具调用 ID、参数、顺序及其他元数据。结构化/多模态工具结果暂不转换，无法满足窗口时仍明确失败。
- 每个结果仅使用与它匹配的调用和该结果生成摘要，避免将同组其他工具的结果混入此调用。按摘要模型输入窗口分片，复用原先摘要的累计工作预算及后台检查点；空历史不触发模型调用。
- 替代文字明确标注是有损摘要，附原始文字 SHA256 和字符数；原始入站正文不改。续接边界仍绑定原始末条消息，防止客户端继续发送原工具结果时无法应用胶囊。生成的工具摘要在历史来源提取中排除，不冒充原文重新索引。
- 压缩质量/索引/上下文/自动候选/后台执行/检查点/智能路由组合 130 项通过，git diff --check 通过。新增 Chat/Responses 超大工具结果分片、工具结构与当前规则不变、原正文不变、原边界追加续接、巨型用户指令不截断和摘要不再索引测试。
- 该证据使用确定性模拟摘要；尚未证明真实 33.9 万 token 会话、连续五轮压缩的事实准确率或模型端工具调用成功率。哈希用于原文校验，不等同于已经实现按哈希自动获取完整工具原文。

### 旧未知来源单独云授权

- 新增账号 history_legacy_cloud_allowed，默认 false，必须先有个人独占确认、历史找回及普通云历史授权；撤销父权限或改成本地限定会清空它，重新开启父权限不会自动恢复。界面提供单独说明和依赖联动。
- 来源索引保留可出云/禁止出云/原始设置未知三态，未知状态同时记入加密正文。查询与发送前读取必须持有单独授权才开放未知来源；不把当前授权写成旧来源本来允许出云，因此撤销立即生效，无需重新索引。
- 仅真正缺失原始隐私字段的记录视为旧未知；明确仅限本地、空对象、null 或不支持的版本全部禁止通过旧历史授权出云。来源重放只能收紧限制，已经禁止的记录不能被后续未知或普通云允许记录升级。
- 索引/授权/投影/索引 worker/管理接口/查询改写组合 112 项通过，JS 语法、设置表单和 git diff --check 通过。含没有单独授权不发、撤销后发送前拒绝、跨账号拒绝、已知本地与畸形隐私记录拒绝及严格布尔值验证。
- 当前功能尚未上线，没有生产索引迁移。本开发版早期测试索引若未记录未知来源字段，仍按禁止出云处理，不能声称它会被自动升级；正式验收从隔离新索引读取真实归档来源。
- 已启动新一轮完整回归，JUnit 证据输出到 /home/ai/github/1Panel-worktrees/context-validation-dxsqjn/full-regression.xml；需等待终态，当前不能报告全量通过。
- 本轮完整回归终端会话为 26620，最近一次轮询仍在运行且已有失败标记；继续轮询同一会话，不因尚无终态而重复启动。只读 docker ps 确认 API local/tail 与 control local/tail 仍使用 1panel-ai-router:context-policy-20260912-r1，本次代码未部署；未重启或修改任何现有容器。

### 完整回归终态与真实 Redis

- 会话 26620 已退出，完整回归 1826 项：1787 通过、35 失败、4 跳过，耗时 261.943 秒，无收集/执行错误。JUnit 在上述 full-regression.xml。35 个失败 ID 全部存在于先前原工作区失败缓存；缓存的 55 条还含过期条目，不能说当前原工作区有 55 项失败。本轮没有新的失败用例名称，不等于全部问题已经修复。
- 已修正其中五项明确的测试基线问题：四项 SSE 时钟预期补上实现已经记录的 last_output_ms，并继续精确验证数值；一项真实审计函数测试补齐运行时 settings 依赖。未修改生产实现来迎合测试。时钟/用量审计两个完整测试文件 28 项通过；其余 30 项仍待核查，修正后尚未重跑全套。
- 新增 tests/test_memory_redis.py，使用已有 redis:7.4.2-alpine 镜像启动专属临时容器，network=none、无端口映射、只读根目录、非 root 用户、无 capabilities，仅通过验证目录中的 Unix socket 访问。两个真实 Redis 测试通过：Lua 续租/过期/持有者隔离，以及两个索引实例争锁、落库和游标续接。
- 首次宿主测试缺少 redis Python 包，按 requirements.txt 的 redis==5.2.1 安装到验证目录 python-deps，未改变系统或仓库依赖；最终 JUnit 为 redis-lease.xml。新增 Redis 用例是在全量收集后添加，因此不包含在上述 1826 项内。
- 临时容器 a606c1d15310b7a589f5cdf6442271682cd7501c94d31d7fb559f999a9614cf8 已正常停止并按 --rm 删除，查询确认不存在；只移除了本次临时 Redis 运行状态，无用户数据删除。验证报告与临时 Python 依赖仍保留于验证目录，生产 Redis 和模型容器均未操作。

### 既有失败的进一步核查

- WorkBuddy 三项旧测试忽略了现有不可变历史保护和工具排序：无已核验旧快照的 assistant/tool 续接必须保留原始消息，只有新会话才搬动态内容；工具 schema 应按名称比对，不按排序前下标比对。改为明确验证原历史、工具定义、最终 history 审计与无正文审计泄露；未改请求处理实现。部署绑定/WorkBuddy 历史/动态前缀三个文件 90 项通过。
- 缓存两项旧测试仍固定两卡：当前注册表要求四个注册实例，启动脚本按 GPU 数生成 CUDA_VISIBLE_DEVICES。健康测试增加两个注册不足和四个注册完整的正反例，继续检查外部缓存指标；脚本契约检查动态设备列表。缓存文件 5 项通过，没有执行 GPU 脚本或修改模型配置。
- 注册表加载测试按当前文件核对为 14 个端点、AI 8 并发/262144 配置上下文，另一个 GLM 端点明确验证 Responses adapter；保留其他端点能力/配置断言。该用例单独通过，配置文件本身未修改。
- 在先前五项基础上，本轮六个旧失败已通过专项复测；剩余 24 个旧失败集中在 test_core.py 的旧 AI 池/视觉/容量场景。完整回归尚未在上述测试修正后重跑，不能报告全套通过。

### 旧 AI 池夹具与真实容量错误

- 给明确测试旧单槽多 worker 池的用例提供 ai_pool_registry()，以已有六 worker 合成数据建立三个部署档位，使用隔离虚构池地址；不再把当前 vLLM 端点当作 ai_pool。生产注册表及当前 vLLM 配置未改变。覆盖忙碌隔离、配置漂移、短请求分散、亲和性、重启缓存代次、长上下文过滤和 Responses worker 绑定。
- 夹具修正后保留的 evaluator 容量失败揭示真实问题：API 把池级 max_concurrency 当成每个单槽 worker 的容量，已占用 worker 仍可接收内部调用。新增统一 _deployment_capacity，前台候选与内部模型准入对 ai_pool 使用每 worker 1 槽；vLLM/Codex 等其他端点保持原配置容量。
- 上述池/内部容量专项 15 项全部通过；后台压缩、查询改写、端点超时及三种后端容量契约组合 43 项通过，git diff --check 通过。未以放宽容量断言解决失败，未重启或调用 GPU。
- 本轮覆盖先前 14 项旧失败及一项原来通过的旧池测试；旧失败尚余 10 项视觉/自动容量/公开模型信息场景待核查。修正后全套回归仍待重跑，真实长对话与上线验收仍未完成。

### 剩余旧失败专项复测

- 四项自动容量测试改用明确的旧单槽池夹具，继续验证本地溢出、全部本地忙后的云回退/429、八并发由六个池 worker 和两个独立节点承接；没有更改这些验收条件。修正旧上游模型名称断言，按目标 provider_model 验证。
- 视觉端点清单增加注册表已有的 Astra/DeepSeek Flash，测试改名为“声明视觉端点”，不把注册声明当成真实能力证明。模型列表和公开响应测试使用当前 AI public_model；仍验证公开视图不泄露内部路由。
- 三项旧池视觉处理测试使用池夹具，验证 1024 缩放但归档保留原图、远程图片不发给池 worker、P40/V100 视觉失败后受控回退。这些限制在代码中明确仅针对 ai_pool，不能套用当前 vLLM 端点。
- 自动容量/声明视觉/模型公开信息组合 7 项通过，池视觉 3 项通过，git diff --check 通过。先前 35 项失败均已在各自专项复测中通过，但尚无修正后的全套终态。
- 第二轮完整回归已启动，报告路径 /home/ai/github/1Panel-worktrees/context-validation-dxsqjn/full-regression-r2.xml；保留第一轮报告，不用新报告覆盖旧证据。
- 第二轮终端会话 97281，最近轮询仍在运行且已通过前 19% 的用例；需继续观察同一会话取得终态。
- 下一步代码复核发现需补测：apply_background 生成胶囊边界使用应用后的末条消息；若末条正是新压缩的巨型工具结果且无追加尾部，应与前台一样绑定原始末条消息，否则后续原始历史重放可能找不到边界。此处尚未修正，不能仅凭普通新增 user 尾部测试宣布后台续接完整通过。

### 后台工具摘要边界修正

- apply_background 改为绑定当前原始入站消息末项的哈希；保存的胶囊仍为实际应用后的精简消息。无追加尾部且最后工具文字被摘要替代时，也能在下一次客户端原始历史重放中定位续接位置。
- 新增 Chat/Responses 两项回归：创建带调用和巨大结果的候选、原入站正文保持不变、边界等于原结果且不等于摘要、使用真正 apply_existing 接续新 user 消息。后台候选/压缩质量/任务存储/上下文组合 55 项通过。
- 这次修正发生在第二轮全量进程启动之后；该进程可能已经导入旧模块，最终报告不能替代此修正的专项结果。最终发布前仍需在冻结候选上再跑完整回归。
- 已开始核对隔离服务入口：build_runtime 支持独立 Settings/Registry/Redis 和 token_counter，API/Control 默认 owned runtime 才负责关闭资源。尚未启动隔离 Router HTTP 服务，不能把此前真实 Redis 测试称作完整 Router 重启验收。

### 第二轮完整回归终态

- 会话 97281 已正常退出，full-regression-r2.xml 记录 1832 项：1826 通过、0 失败、0 错误、6 跳过，耗时 257.428 秒。
- 跳过为 H3 MCP 收集、现有 GGUF 库缺少 typed array metadata 的一项、旧 local_pool 的两个可选 Redis 用例、本次 history 的两个可选 Redis 用例。本次 history Redis 两项已有独立真实 Redis 报告通过；旧 local_pool 两项尚未在本轮隔离实例中运行。
- 此终态证明第一轮 35 项失败在第二轮测试快照中全部消除；后台工具摘要边界修正及新增两项仍以上述独立 55 项回归为证，最终冻结候选需再跑全套。真实摘要质量、Router 进程重启和生产配置/验证仍待完成，不以 CPU 全套零失败替代交付验收。

### 隔离 Router HTTP 进程重启验收

- 新增 tests/test_context_service_restart.py，实际启动独立 API/Control Uvicorn 进程，通过 127.0.0.1 随机端口及真实 HTTP 请求交互。后端为只返回确定性结果的本地 HTTP 测试服务，不是进程内 MockTransport，也不是实际语言模型。
- Redis 使用新的临时无网络容器和 Unix socket；配置、归档、索引、路由追踪及独立生成的凭据均与生产隔离。环境清除继承的 AI_ROUTER_* 后显式设置测试值，注册表只有测试端点，不连接任何实际模型后端。令牌计数使用 SimpleTokenCounter，仅证明流程，不证明模型窗口。
- 实际控制 API 创建独占测试账号及密钥，发送源会话，等待后台索引，SIGTERM 正常停止 API 并启动新进程，用新会话检索原记录。确认账号实例 ID 相同、PID/boot ID 改变，原请求 ID 和配置值进入实际转发正文；新请求的原始归档没有被回查内容污染。
- 最终复测通过：原 PID 3153521，重启 PID 3153960；boot b83050a552e347019b33bd75dbe227d4 -> 3dcaf7791e35428d8745d1a451737423；来源请求 6e06e5ed2d9f4a87a056afed68dfa167，重启后找回请求 1d26e88e6ccb49639519075ab8d31a92。
- JUnit：/home/ai/github/1Panel-worktrees/context-validation-dxsqjn/http-restart.xml；配置、日志和 evidence.json：同目录 pytest-of-ai/pytest-2/test_router_http_history_survi0/。早期失败分别为测试配置缺少 bootstrap policy、测试账号默认公开身份不允许内部模型；修正测试配置后通过，没有放宽运行时授权检查。
- API/Control 子进程已全部退出；临时 Redis 13252ed190be1328c5ef36e7b591ddafc1ede097d614166ac3cbeae87ae60b6f 已正常停止并按 --rm 清理。生产未改动。此证据仅覆盖非流式 Chat 的历史索引与重启找回；后台压缩任务重启、真实 Responses/SSE、真实模型摘要质量及生产滚动重启仍待验证。

### 后台候选跨 API/Control 进程重启

- 扩展同一真实 HTTP 验收，使用控制 API 授予测试账号压缩权限、开启后台压缩并指定摘要模型，再从归档请求创建任务。上游实际接收带 operation ID 的摘要 HTTP 调用，任务生成 ready 候选。
- 同时正常停止并重启 API 和 Control 后，任务 ID、ready 状态及调用计数保留；再观察一个完整 worker 轮询周期，摘要上游仍只有一次调用，没有重复生成已完成候选。
- 初始合成历史约 3286 token，全被分到最近保留消息，无法缩短而被拒绝；已通过实际分区函数核实 older=0/recent=6。改为约 17261 token 后 older=2/recent=4，进入真正摘要路径；没有放宽不收敛保护或更改生产压缩逻辑。
- http-jobs-restart.xml 最终通过，配置、日志与 evidence.json 在 context-validation-dxsqjn/pytest-of-ai/pytest-4/test_router_http_history_survi0/。测试使用确定性本地摘要服务，不证明真实模型质量；本轮证明已完成候选跨重启保留，不覆盖摘要调用在途崩溃的未知结果恢复。
- 所有测试子进程已退出，临时 Redis 1dccb0ec2356f0a46641c061e22a4552d2d3654f1d92bc21d987950329c3119f 已正常停止并自动清理，报告和测试数据库保留；生产服务未操作。

### 真实摘要格式诊断与 Flash/Pro 短文本验证

- 首轮 live-smoke-1 已终止：Flash 返回 HTTP 200，但摘要校验失败，任务 9bac508f11bc4cb49317d9e0b2b6e136，单次调用约 9.41 秒。旧错误记录仅有 summary_http_200，未保留具体失败类别，无法追溯究竟是 JSON、字段类型还是空内容；不把推测当成已证实原因，也未重发该任务。
- 静态检查发现摘要提示词只要求字段名称，而校验要求每个字段必须为数组。提示词现明确数组与空数组要求；校验仍拒绝错误类型、空摘要和截断。错误新增固定分类，在加密步骤及首个失败证据中保存；未知诊断字符串降为通用分类，不保存上游正文或异常文本。
- 摘要质量/任务存储/检查点/worker/后台候选组合 58 项通过；新增字段格式提示契约、各类失败类别、原因跨重启保留与任意文本不落错误记录检查。JUnit 为 context-validation-dxsqjn/summary-validation-reasons.xml，git diff --check 通过。
- 新任务 live-smoke-2 使用同一虚构短文本及修正后的提示词，经 RoutedCompactor 权限、容量、预算与持久步骤执行。独立目录/账号/数据库，InMemoryStateStore，未修改生产配置；预留预算按 Router 现有价格配置不超过 0.10 美元，实际供应商账单未核对。没有调用 GPU 模型。
- Flash provider_model=deepseek-v4-flash，任务 6d308438d3bf4d28a366287b03b57297，11.79 秒，ready，调用 1 次；Pro provider_model=deepseek-v4-pro，任务 c21fd77856cd4b3587b1c336aede079d，4.71 秒，ready，调用 1 次。均保留 ORBIT-718、1739、/srv/orbit/example.log 及禁止重启服务的约束。输入估算各 190 token，摘要输出估算分别 89/68，不是供应商实际用量。
- report.json 位于 context-validation-dxsqjn/live-smoke-2/，首轮报告单独保留。上游目录返回 deepseek-flash 而既有配置使用 deepseek-v4-flash；本轮真实调用证明后者当前可用，没有改注册表或生产转发名称。仅凭这两个短样本不能比较模型优劣，也不能证明原首轮错误唯一由提示词导致。
- 真实 33.9 万 token、多轮摘要质量、完整协议/在途崩溃恢复、最终冻结回归及生产滚动重启与参数配置仍未完成；无提交、推送、PR 或部署。

### 可复现的 33.9 万 token 验收输入

- 新增 scripts/context_acceptance.py，生成纯虚构语料：40 个服务最终端口问答、4 次废弃端口更正、系统约束、一组完整工具调用和含唯一最终故障编号的超大工具结果。逐行日志含变化的节点、事件编号和摘要，不是单句重复填充。
- 用现有 /opt/1panel/ai-router/tokenizer 和真正 HuggingFaceTokenCounter 二分计数，得到 339008 token、10090 行日志。仅运行 tokenizer，无模型推理；生成时最大候选超过 tokenizer 标注的 262144 窗口会警告，这是计数输入，不表示向本地模型发送了超窗请求。
- 固定 transformers==4.49.0 安装到隔离验证目录 tokenizer-deps，未改变仓库或系统依赖。pip 提示现有其他工具依赖不兼容，包仅通过该命令的 PYTHONPATH 生效；本次实际 tokenizer 加载和计数已通过，不把此隔离目录用于生产运行。
- 语料文件 context-validation-dxsqjn/long-fixture-339k.json 包含正文、问答期望、计数及 tokenizer 三个文件 SHA256；正文 SHA256 为 3df12481feaa28d43bc22cbe1ad09964e9ebd8e02e55d748dc5800cc78b9a649。该计数是 Router 口径，不是 DeepSeek 供应商实际用量。
- 五项生成器测试通过，覆盖确定性、目标计数、四个纠正项、工具配对及生成规模边界；初次收集遇到 scripts 名称冲突，改用显式文件模块加载后通过，git diff --check 通过。
- 尚未将这份长语料提交给模型。接下来需在独立预算下经 Router 准入执行真实 compact 路径，检查系统约束、工具配对、最终端口/故障编号及连续五轮退化；不可用生成器测试代替摘要质量验收。

### 真实长输入 compact 首轮完成

- 扩展受控烟雾入口：--fixture/--tokenizer 必须同时提供，核验正文 SHA256 与实际 Router token 计数；长模式调用真正 compact(target_context_tokens=272000)，不是直接对全文调用 _summarize。新增任务心跳，独立账号/状态/预算，按配置价格总预留上限 1 美元，每模型任务最多 600 秒；不自动重试或失败换模。
- 47 项压缩质量/检查点/worker/语料测试通过，报告 context-validation-dxsqjn/long-preflight.xml；语法编译与 git diff --check 通过。
- 实际调用已完成，终端会话 28854 正常退出。报告及加密任务数据库位于 context-validation-dxsqjn/live-long-1/。Flash 任务 cf6276a1c71742f4b81ce82f3867629d：339008 -> 2051 token，40.94 秒；Pro 任务 20542246a19646c7936d74cbf49e41fa：339008 -> 1941 token，75.06 秒。两者各两次实际摘要调用，分别处理较早历史与巨大近期工具输出，任务均 ready、无未知操作。
- 两者累计摘要输入 Router 计数各 339199，输出各 1827/1701；以上不是供应商实际用量或最终账单。调用经过 Router 权限/健康/容量/预算以及持久步骤，运行存储为独立 InMemoryStateStore，不是本轮生产多实例/Redis 验收。
- 自动检查原系统消息不变、工具调用参数/ID及结果配对、E_FINAL_782 保留。人工检查两个实际输出均保留 40 个服务端口记录和四次更正：003/011 在摘要，023/037 保留为后续原始 user 消息。Pro 同时列旧值与明确更正，Flash 将较早两项合并为最终值；不能只搜到旧值就判错，也不能以字段存在代替真实问答准确率。
- 本轮证明两模型在该合成 33.9 万 token 样本上完成一次真实压缩。连续五轮、实际模型读取摘要后的 40 题问答、遗漏细节检索、旧实现对照、完整协议/崩溃恢复与生产滚动重启仍待完成；未更改生产配置、提交或创建 PR。

### 连续五轮实测进行中与盲测评分

- 烟雾入口新增 --rounds 1..5，逐模型顺序执行；每轮沿用上一轮实际精简历史，追加 ORBIT-000 最终端口更正及带独立 call ID 的巨型工具输出，再执行真实 compact。每任务预算保持原上限，总云预算按现有价格配置封顶 5 美元，独立运行数据库，不是供应商账单。
- 实际进程会话 3294 仍在运行，目录 context-validation-dxsqjn/live-five-rounds-1。Flash 五轮已经生成 validated_summary，耗时分别 34.01/26.47/28.84/26.42/39.32 秒；Pro 尚在执行，须轮询同一会话，禁止因没有完整报告而重新启动。
- Flash 前四轮输出分别 2222/2838/3338/3788 token，证明旧系统摘要随轮次积累。目前保留它们避免丢失既有事实；此现象不等于无限轮次可持续，仍需要最终问答和增长控制评估。
- 新增 scripts/context_quality.py：构造问题时仅发送服务名及问题文本，不发送期望答案；固定 40 项严格评分，按末轮端口更正计分，拒绝重复/额外/非法答案，漏答算错。三项评分测试通过，合并生成器/摘要质量 23 项通过，JUnit 为 context-validation-dxsqjn/five-round-preflight.xml。
- 烟雾入口新增 --qa-report，要求两模型完成第五轮并校验语料 SHA256，使用 AnswerCompactor 经同一 Router 权限/容量/预算/检查点路径读取精简历史作答，记录来源任务与盲测得分。此模式已实现、尚未发起实际问答调用；独立 QA 配置预算 0.10 美元，不改变摘要模型或上游注册表。
- 当前只能报告 Flash 五轮结构/约束检查通过；两模型完整五轮终态、实际问答 >=95% 及其他交付门槛仍未完成。生产未修改，无提交或 PR。

### 五轮压缩终态及真实 40 题盲测

- 会话 3294 正常退出：live-five-rounds-1/report.json 共 10 个 ready 任务、20 次实际摘要调用，Flash/Pro 各连续五轮结构和关键约束检查通过。第五轮输出分别 4232/3099 Router token；第五轮任务分别 8f785e81ef164865b1084887f9308b8c、f32b1256d6c64095b243b2c09882d41b。
- QA 会话 31391 正常退出，使用两个第五轮结果而不是原始全文；问题仅含服务名与问题，不发送标准答案。两模型分别 40/40、无重复/非法答案，包含 ORBIT-000 第五轮改为 28005 以及原四个端口纠正，达到该有限测试集 >=95% 门槛。不能推论任意任务或无限轮次质量保证。
- QA 目录 context-validation-dxsqjn/live-five-qa-1：report.json 含答案与评分，provider-evidence.json 仅记录请求/模型标识、指纹、状态和整数用量，不记录响应正文、隐藏推理或凭据。Flash QA 任务 e609c0cc3c6344649ca3bd559dfb9034，6.48 秒；Pro 任务 147614cefcaa4136a5fe61255b2329fd，19.23 秒。
- Flash 上游实际返回 model=deepseek-flash，request ID ec38465f-b1fa-4322-a7c4-c34b4573e88d，fingerprint=aeb56401ca74e127821c4f9126dcb669，usage 输入 5213/输出 1767；Pro model=deepseek-v4-pro，request ID 6427588a-3c9f-4cac-971f-4c042ff9f65e，fingerprint=a307abda487cd1b463329ccb945ce396，usage 输入 4076/输出 2552。这些是真实 QA 用量，不回填为此前长压缩调用用量，账单仍未核对。
- 等待长测试期间修复手工归档压缩任务的来源策略校验：只接受 version 为整数 1 且 local_only 严格 False 的出云许可，非法类型/未知版本拒绝出云，避免字符串策略触发 .get 异常；索引来源同步拒绝布尔 version=True 被 Python 当整数 1 接受。来源/控制/归档组合 43 项通过，JUnit 为 context-validation-dxsqjn/source-policy-validation.xml。早期测试命令路径与 ArchiveReader 导入问题已纠正，未修改生产数据。
- 多轮旧摘要积累的增长控制、遗漏细节回查/中文改写真实验收、旧算法对照、可配置任务预算、完整协议与在途崩溃恢复、最终冻结全套及生产滚动重启/配置仍待完成。生产无变更，无提交、推送或 PR。

### 丢失细节回查与错误编号误匹配修正

- 新增回归先复现真实缺陷：查询 NO_MATCH_98543 的配置路径时，原搜索会因“配置路径”等公共词命中已有 E_MEMORY_782 记录并生成注入片段。此前仅用孤立不存在编号测试无匹配，未覆盖带常见上下文词的情况。
- 检索现在提取原问题的完整规范化编号/路径标识，有显式标识时候选必须至少包含其中一个完整标识，不能只靠拆词或其他通用词匹配。查询改写后的第二次检索仍携带原问题标识约束，不能通过删除未知编号回退到无关历史。没有扩大条数/时间/token 上限或变更授权规则。
- 新增 10090 行虚构日志经真实 archived_sources 分块和加密 MemoryIndex 建索引，再经 prepare_recall 找回事件 004321 的 SHA 摘要值；保留来源请求 ID，<=6 段/窗口10%限制。生成投影后排除来源会话，recall_for_send 必须返回原请求且拒绝注入。
- 检索/索引/查询改写组合 67 项全部通过，JUnit 位于 context-validation-dxsqjn/recall-identifier-validation.xml，git diff --check 通过。首次新增未知编号回归失败，修复后通过；不是以放松断言解决。
- 本轮真实组件为加密 SQLite、来源分块、检索及注入准备/发送前校验；账号/计数在测试夹具中隔离，未发起新的真实模型调用，因此不能称为实际模型读取找回片段后的回答验收。中文改述的真实查询改写、完整 HTTP/协议回查及剩余发布门槛仍待完成。

### 真实模型读取找回片段后的回答验收

- 烟雾入口新增 --recall，必须提供原语料/tokenizer 及完成的五轮报告。用真正 ClientAccountManager 创建独立个人测试账号并显式授予历史/云权限，使用真实 archived_sources、MemoryIndex、prepare_recall、recall_for_send；不启动后台 worker、不读取生产归档。原语料为已固定的虚构日志，测试索引位于独立状态目录。
- 先断言第五轮摘要不含事件 004321 的摘要值，再经检索准备和发送前复查得到两段原文。每模型 added_tokens=3862，<=4096 且符合目标窗口10%限制；补充仅进入待发送副本，原问题与历史保持不变。模型只接收已有第五轮内容与检索片段，没有收到正确答案或整份原始日志。
- 会话 18370 正常退出，context-validation-dxsqjn/live-recall-1/report.json 记录 Flash/Pro 均返回 event_id=004321、digest=fe2592b42a72，严格比较通过。任务分别 1a82b6df12f24156918ef0b711111a9a、0b67e2db707b4b15aec0484a1b024ad7，实际模型阶段耗时 2.79/11.29 秒。
- 实际上游请求 ID：Flash 9afcfdc0-c804-4cb6-a7dc-8c755c17b810；Pro 2643fda6-98a3-4b1e-b84e-6aceaec7009b。provider-evidence.json 记录实际模型、指纹和 usage：Flash 输入7095/输出439，Pro 输入5958/输出534。独立配置预算0.10美元，最终账单未核对。
- 检索/授权/索引组合 91 项通过，JUnit=context-validation-dxsqjn/live-recall-preflight.xml。此真实模型测试走 Router 内部准入与检查点，身份注入为测试空转换，不等于完整外部 API/Responses/SSE 或多实例实时撤权验收；这些仍须分别证明。
- 现在已证明这个遗漏细节可按需找回并被真实模型正确使用；不意味着原文自动全部进入上下文，也不代表中文语义改述、无匹配、无限摘要轮次或生产部署等剩余门槛已完成。生产无变更，无提交或 PR。

### 后台任务预算设置贯通

- 新增 compaction.background_limits：max_seconds=600、max_calls=32、max_input_tokens=1000000、max_output_tokens=64000。默认保持原安全上限；当前可调低、不扩大，输出预算至少8192以容纳单次预留。后端拒绝未知字段、布尔/字符串/非整数及越界值。
- 自动提交和手工归档提交都把预算写入任务参数；任务存储规范化并固定快照，去重包含预算，公开任务元数据返回该快照。旧无预算字段任务读取使用既有默认上限，已累计用量不清零。修改设置只影响新任务，旧任务仍按其快照执行。
- 检查点压缩器继承任务预算，共享递归工作量检查、持久 dispatch、超时等待、重启领取和候选发布均使用同一快照。没有只增加表单而保留固定执行阈值；全局并发仍为1，前台压缩默认安全上限保持原状。
- 设置页面新增四个数值项、范围和快照说明，render/collect 往返读取保存，并有前端范围校验；复用原页面布局，不改设计系统。
- 最终组合76项通过，JUnit=context-validation-dxsqjn/background-budget-settings-r2.xml；其中新预算测试17项，覆盖设置拒绝非法值、调用/输入/输出超额拒绝、参数对象修改不污染任务、数据库重开、短时限与过期租约恢复、模型发送前拦截。前一次报告 background-budget-settings.xml 为75项，保留不覆盖。
- node tests/test_context_policy_ui.cjs、node --check ai_router/static/app.js、git diff --check 通过。完整浏览器及真正服务保存/重启后的新预算联调尚未执行；最终冻结全套和生产发布等剩余门槛仍在，不把专项通过等同上线。

### 实际 API/Control 重启后的预算快照验证

- 扩展 tests/test_context_service_restart.py，通过真实 HTTP 保存预算、创建并完成任务、改预算，同时停止并重启 API/Control；恢复后旧任务仍保留30秒/1调用/50000输入/8192输出，新设置45秒/2调用/100000输入/16384输出由 API 读回，并用于创建不同任务。两个任务均 ready，各实际接收一次摘要 HTTP 请求，旧任务未重发。
- 首次 http-budget-restart.xml 失败原因是测试把 PUT 设置替换接口当作局部更新，遗漏后台开关导致默认关闭；改成先 GET 完整设置、修改预算后 PUT，未改变接口语义或生产实现。http-budget-restart-r2.xml 完整通过，配置/日志/evidence.json 在 context-validation-dxsqjn/pytest-of-ai/pytest-6/test_router_http_history_survi0/。
- 旧任务1f707c21dc5d4c73a5b96ce9fc856d4e，新任务d6874387fc62426ebca8aa41918bc019；摘要 operation IDs 85cf4c430f1f4581b3c79f19fe2e3729、2c3239afab7a4e5281726322b5a2dcca。原会话请求736e8a53230a46d2a98108a1e8854a43，重启回查请求e5ce43ca7b0b46b48081b1d63030be89。
- 使用真正 Redis（独立无网络 Unix socket 容器）及本地确定性 HTTP 假模型，不是实际语言模型质量证明。所有 API/Control 子进程已退出；临时容器dd7d45f5c34888bf637ede19ccdc41e89c4f3ae675e148069f76fc96665b9ddf已停止并自动移除，数据库报告保留，生产未操作。
- 浏览器验收扩展预算输入/采集/越界提示和桌面手机截图；首轮因测试未关闭账号对话框，点击背景设置被正常模态层阻挡，已改为先按 Escape 关闭再切换页面。第二轮浏览器会话35764仍需终态，预览服务会话93099/PID3325965仅绑定127.0.0.1:14871，须测试结束后正常停止。

### 预算浏览器验收终态

- 第二轮35764失败因设置默认“简单”模式隐藏高级表单，测试补充正常选择“高级”，未修改页面显示逻辑或强制点击隐藏元素。第三轮38262正常通过：账号授权依赖、索引/任务状态、预算采集和越界识别、桌面1440x1000与手机390x844可操作性，无页面 JavaScript 异常。
- 截图位于 context-validation-dxsqjn/browser-budgets-r3/，已查看 context-budget-mobile.png：四项预算纵向排列、字段可读且未横向溢出。浏览器只访问有隔离标记的127.0.0.1预览服务，跨源请求被阻止，未向生产保存设置。
- 已向确认的预览PID3325965发送SIGTERM，会话93099终态退出码143，进程查询确认不存在。Redis和API/Control测试均已停止；持久测试报告、数据库与截图保留。此结果与上述实际HTTP预算保存/进程重启证据互补，不替代完整生产部署验收。

### 在途调用进程崩溃与未知结果恢复

- 扩展真实HTTP进程测试：本地假模型收到摘要operation后阻塞，确定dispatch已发生；对测试API/Control同时SIGKILL，随后释放假模型响应，再启动两个新进程。使用实际60秒任务租约，不缩短租约或直接改数据库状态。
- 首次 http-inflight-crash.xml 未通过，因为只杀API时Control也可能是任务执行者，任务在存活Control中正常完成。根据 runtime.start 确认两个服务均启动worker，修改测试同时中断两个可能执行者；没有改变生产worker架构。该首轮不是未知结果恢复失败的证据，而是故障注入未覆盖实际执行者。
- 第二轮会话96025正常通过，报告 context-validation-dxsqjn/http-inflight-crash-r2.xml；日志/状态/evidence.json在pytest-of-ai/pytest-8/test_router_http_history_survi0/。API PID3365548->3366665，Control PID3365591->3366706。
- 任务d925a53871e94ea8909fbbd8c08e193b恢复为needs_context，error=upstream_outcome_unknown，calls=1；未知operation=10058da0074f4e4a90ebfbbc103fbec4。再观察完整worker轮询仍未重发。管理员经真实reconcile接口记录已观察到假上游结束并放弃结果后，状态cancelled、calls仍1、未决列表为空；这仍是人工确认路径，不是自动查询供应商状态。
- 所有子进程已退出，隔离Redis容器51d9f56059c0238cce52406bd37874b3ae953a98b24e0923a39d01ebf13070df已停止并自动移除，持久报告保留；未操作生产、GPU或真实云模型。
- 第三轮全量回归会话86569仍在运行，输出已超过77%且暂无失败标记，需继续等待同一会话终态；报告目标context-validation-dxsqjn/full-regression-r3.xml。该轮启动后只修改可选进程测试，无生产代码变更。

### 第三轮完整回归终态

- 会话86569退出码0。full-regression-r3.xml共1867项：1860通过、0失败、0错误、7跳过，耗时259.133秒；git diff --check通过。
- 跳过包括H3 MCP收集、现有GGUF typed-array兼容用例、一个可选真实进程/Redis重启用例、两个旧local_pool Redis用例、两个history Redis用例。本次真实进程/Redis崩溃与预算验收已有上述独立通过报告，history Redis已有前轮真实证据；两个旧local_pool Redis用例仍未在本轮独立运行，不把跳过称作通过。
- 第三轮覆盖最新生产代码，运行中修改的可选进程用例另以http-inflight-crash-r2.xml证明。本阶段仍未完成中文改述真实改写、完整Responses/SSE回查验收、旧算法对照和实际生产发布/重启/配置等剩余门槛，因此尚不提交PR或宣告目标完成。

### Chat/Responses/SSE 回查与可选 Redis 验收

- 隔离HTTP假模型增加原生Responses完成响应与SSE生命周期事件，以及Chat流式chunk/[DONE]。真正API/Control子进程新增三种请求，均验证实际转发正文含历史来源和值、响应终态完整、归档complete且received_body没有注入片段或历史答案。
- 会话5062完整通过，http-protocol-matrix.xml及pytest-of-ai/pytest-9/test_router_http_history_survi0/evidence.json为证：Chat SSE请求bdbeb3b0639749549385f8c0e103fb29，Responses非流式bcd484b89c9b45b2a67c7bc94dc2b9f3，Responses SSE请求28c6a94dd01e469ea2b8be9c56770c1c。原先非流式Chat/预算重启/在途SIGKILL未知结果恢复也在同一测试中再次通过。
- 此处SSE走实际网络与流式处理/归档代码，假模型一次写出完整事件序列；不证明真实供应商逐字延迟或协议能力，也不把模型声明当成验证。真实模型读取回查片段已由前轮独立Flash/Pro证据证明，生产外部协议最终验收仍待上线后执行。
- 四项可选Redis回归在同一隔离容器的独立DB2/DB3运行且全部通过，报告all-optional-redis.xml：旧local_pool跨客户端原子分配、verified_history并发候选边界、history租约持有者隔离及双实例游标续接。此前两项旧local_pool“未运行”缺口现已补齐。
- 所有测试子进程退出；临时容器6ee7cd2041ea175629183702f0ef6923b58f17d47e5778f651d382e929ef116d已停止并自动移除，报告/数据库仍保留。没有操作生产、GPU或新增云模型调用。剩余中文改述真实查询改写、旧算法对照、生产发布/配置/重启及最终线上验收仍未完成。

### 中文查询改写实际能力与边界

- 新增受控 --rewrite-only，真实 Router 账号/模型准入/预算/3秒请求时限/256输出token，索引仅含虚构“巡检日志的摘要校验码用于确认内容完整性”。模型只看到问题，没有历史正文或标准关键词。原问题在索引中无可靠匹配，调用一次后用返回词重新检索。
- live-rewrite-1 对“那份检查记录的指纹怎样检索？”返回泛化查询动词，1.91秒但无匹配。随后增强提示词，要求业务名词同义扩展、去除问句套话并设temperature=0；live-rewrite-2输出校验失败且供应商usage显示completion_tokens=256，旧记录缺少finish_reason，不能断言具体截断原因。
- 根据官方 https://api-docs.deepseek.com/api/create-chat-completion/ 和 guides/thinking_mode/ 文档，仅为DeepSeek短关键词调用增加thinking disabled与JSON输出，并在Router能力匹配中要求json_object。其他供应商、长摘要和正常对话不改变。live-rewrite-3能返回完整JSON，1.35秒，但歧义“记录的指纹”仍未找到该来源；不继续把无匹配结果宣称为成功，也不扩张时限或预算。
- 另增语义明确的问题“那份日志文件的哈希值在哪里？”，没有更改原来源。live-rewrite-file-hash-1中Flash返回“日志文件 哈希值 校验和 摘要 指纹”，1.36秒，原始0命中->1来源，实际中文同义改述样例通过；Pro返回“日志文件 哈希值 位置”，1.27秒仍无命中，未通过同一召回样例。报告退出码1忠实反映不是两模型全通过。
- 69项改写/检索/索引回归通过，query-rewrite-controls-r2.xml；新增provider-specific参数检查和提示词合同，保持未知编号不能被改写抹除、至多一次调用和失败释放资源。新增finish_reason白名单元数据记录，不记录隐藏推理或响应正文。生产未变更。
- 当前证据支持Flash用于已测中文改写样例，不能推论通用语义召回率或Pro同样通过。歧义/无匹配应保持可观测跳过，不猜测历史。新增生产代码变更仍需最终完整回归；旧算法对照、生产发布/配置/重启及线上验收仍未完成。

### 线上旧算法结构对照与发布前只读盘点

- scripts/context-baseline-compare.py 只读取线上已安装压缩模块，用同一339008 token虚构夹具测试。摘要阶段替换成空数组的理想下界，不发送模型请求。线上模块SHA256为290a2ca98621ede5717a3152c4c238a2522ab2381c76a5b974b11cbd6bf3ed41。
- production-baseline-compare.json显示，仅旧算法保留的最近消息就有336600 token，超过旧40%目标108800，也超过新版60%目标163200。即便早期历史摘要几乎为空，旧算法仍拒绝，理由为generated migration capsule exceeds 40% of the destination context。这证明需要处理超长最近工具输出，不能只换摘要模型；不是旧模型语义问答成绩或新算法通用质量证明。
- 四个线上API/Control容器仍使用1panel-ai-router:context-policy-20260912-r1。只读查询两个API实例均running、draining=false、active_request_count=0；该值仅为现场快照，发布时必须重新排空检查。
- 从现有容器以SQLite只读模式检查/training/conversations.sqlite3：12392710144字节、12972条training_records、WAL模式，只有原training_records/sqlite_sequence/training_metadata三张表。没有读取或导出对话明文。新版本会添加事件表、触发器与旧行初始化，部署前须明确数据库备份及兼容回退安排，不把代码隔离等同数据隔离。
- 新增生产代码后的全量回归R4已启动，报告目标context-validation-dxsqjn/full-regression-r4.xml，尚待终态。此阶段没有构建候选镜像、保存线上设置、数据库写入、排空或重启生产，也没有提交、推送或PR。

### 设置替换语义的预检一致性修复

- 复核发现PUT /api/settings按defaults+请求替换保存，但新增后台依赖校验原先按当前运行值+请求合并检查；省略字段时两者可能选择不同摘要模型或云授权。没有改成PATCH，而是在Settings增加preview_runtime，复用实际保存的合并、旧权重兼容和字段校验逻辑；上下文目标与后台依赖检查都使用此待保存值。
- 新增HTTP回归以旧运行摘要模型与默认模型不同为例，断言依赖检查收到默认模型，检查拒绝时文件未变；另验证预览值与实际保存后读取一致。设置/预算专项20项通过，settings-replacement-preflight.xml，git diff --check通过。
- 此修复发生在R4运行中，R4不能代表冻结后的全部代码，需终态后再跑最终冻结回归。仍未进入生产写入或发布。
- R4会话84401现已正常退出码0，JUnit full-regression-r4.xml可复查；不是冻结后全量证明。Codex Adapter worktree与线上文件SHA256一致，均06d636ae38f86f5f80a635b93e748244dc10af73ea09298f3b3b8dc6c412b56e，不需要为已有相同内容重启该服务。

### 冻结回归与候选打包边界

- R4最终共1869项、1862通过、7跳过、0失败/错误；R5在设置替换修复后启动，session55766，报告full-regression-r5.xml。启动时ai_router/config/tests/scripts的排序文件哈希清单总SHA256为fc5f63af8c78ef843cad5ade4eaf9b75a3a79bd54a9a4a54d43b3abb34a63cdf；结束后须再次核验，文档不包含在此代码冻结范围。
- 只读比较生产API镜像与worktree：生产没有独有源码文件，新版增加10个后台任务/记忆模块，既有模块差异限定于本功能修改。config/registry.yaml不是本次修改，但与生产存在差异，不能直接用worktree整包覆盖生产。
- 注册表仅两个模型的safe_context_tokens和context_status不同：线上zhipu-glm-5.3与zhipu-glm-5.3-flash均1000000，worktree分别32768/262144。后续候选必须保留线上注册表及既有Compose覆盖链，不回退GLM参数。固定源码单元回归不能替代使用实际生产注册表的候选/上线验证。
- 尚未收到上一条危险操作确认。继续仅做隔离回归和只读盘点；不把自动目标续行当作新增生产数据库迁移、配置写入或重启范围的明确确认。没有构建、重启、提交、推送或PR。
- R5会话55766终态退出码0：1870项、1863通过、7跳过、0失败/错误，259.287秒。结束时冻结范围哈希仍为fc5f63af8c78ef843cad5ade4eaf9b75a3a79bd54a9a4a54d43b3abb34a63cdf，确认测试中未修改源码/配置/测试/脚本。设置界面Node检查与git diff --check通过。
- 7项跳过为H3 MCP收集、GGUF typed-array库支持、真实进程重启及4项Redis用例；后5项前轮有独立隔离真实进程/Redis通过证据，本次未重新运行，不能记为R5通过项。R5没有执行生产数据库迁移、真实线上重启或新版本线上模型请求，不能据此完成目标。

### 用户确认后的生产滚动发布与首轮真实验证

- 用户明确回复“确认部署”。操作文件在/home/ai/.local/state/ai-router-acceptance/20260913-context-memory/；镜像继承核验过的原context-policy-20260912-r1，仅复制本功能源码/defaults/UI，保留线上registry。首个构建使用裸sha256引用失败，改用哈希核对一致的本地标签后构建成功，没有替换生产基础镜像或拉取依赖。
- 使用SQLite在线backup生成/training/backups/context-memory-20260913-r1/conversations.sqlite3，12411117568字节、12995条、quick_check=ok；同目录保存training.key和settings.yaml，未导出或打印密钥。/data/backups下无权限，配置备份改存已受保护的归档备份目录，未修改原目录权限。
- 四个Router实例均已更新为r1，image sha256:9492ed8927801dea6fc4cd8f75bb344d3f1517d9f1189ed31d452edbd1a4623a，restart_count=0。API排空在途0后替换，首轮启动初始化期间端口暂未监听，随后running/draining=false。原Compose覆盖链、命令、网络、挂载和环境保持一致；Redis/LiteLLM/Codex Adapter/模型服务未重启。四实例源码与worktree一致，仅有特意保留的生产registry差异。
- 生产新增training_history_events与两个触发器已验证，history_events_seeded=1。压缩设置保存读回：background_enabled=true、history_query_rewrite_enabled=true、model_id=cloud-deepseek-v4-flash、600秒/32调用/100万输入/6.4万输出；保留explicit_only和context_policy.mode=compact，未修改现有账号历史授权。
- 临时独立账号真实Chat非流式736bd473db154bbdbe58ffad7db77450和Chat SSE efa984fb302d44f6b91e21b40d5433c2均200且精确标记匹配。首个Responses 91e3b6876dd34f8e972acc02ed00aaad最终消息也精确匹配，但测试脚本错误拼入非最终输出项；修正仅提取message/output_text后，Responses 6bd40292256744b7b6b4470f9b42b49a和Responses SSE 27a3f00dbd9e424dbf2acc64faad0d9a均completed且精确匹配。报告protocol-report.json保留原失败，responses-report-r2.json为修正后结果；两账号均停用、密钥撤销。
- 真实长输入请求661c71b937db4304bdaffd709d414ea8成功，8.9秒，DeepSeek usage输入242633/输出8；此夹具Router本地分词约339k，不与供应商分词计数混同。随后管理接口创建压缩任务返回503，未成功创建任务，临时账号与密钥已关闭，不能宣称后台链路通过。

### 只读归档挂载的WAL生命周期修复

- 现场在Control容器复现ArchiveReader读取报sqlite3.OperationalError: unable to open database file。文件权限和密钥正常，但API每次关闭最后一个SQLite连接后清除WAL/SHM；数据库本身仍是WAL模式，Control只读挂载无法重建辅助文件。这是隔离测试此前没有覆盖到的真实挂载差异。
- TrainingArchive新增空闲连接持有WAL文件，不持有读事务；runtime停止worker后关闭连接。没有使用immutable读取活数据库，没有把只读挂载改成可写，也没有放宽历史权限。
- 35项专项通过，wal-anchor-targeted.xml。两个真实隔离容器共享测试目录，写端完成后空闲，另一容器只读挂载读取成功；测试脚本test_readonly_wal.py，数据readonly-wal-52762mje保留，临时writer及reader容器均已退出移除。
- 修正版r2已构建，image sha256:bf60547ff6295db23ad16d250f059b40edda05d1d5af9c238f3a5ddca8b831c5，尚未部署。全量R6会话88896运行中，报告full-regression-r6.xml；冻结范围SHA256=0430d895a48365d25714800b2bd6cf4b0ea6c01e2d4e235e5289f9eeeb7a5113。等待终态后再滚动部署r2并重做后台压缩/应用验证。目标尚未完成，无提交、推送、PR。

### r2上线、真实候选生成与续聊关联缺口

- R6唯一失败为test_instance_status简化运行对象缺少training字段，不是生产归档失败。补齐training=None后22项生命周期专项通过；R7全量1871项、1864通过、7跳过、0失败/错误，261.797秒。
- 四实例已在同一授权范围滚动更新r2，源码匹配worktree（仅registry保留线上差异）、restart_count=0。在只读生产Control容器重新读取原失败请求661c71b937db4304bdaffd709d414ea8成功，保持只读挂载。HTTPS控制端读回设置一致且证书验证通过。
- r2真实长请求6c11d233d0ca48e8abe86eac22a0db1c成功，后台任务ed84d819efb44043ae34949ec2efd2bb以2调用、339237估算输入/2590输出、41.40秒进入ready。后续829622d155b040b9adbd33138e507b35回答正确，但实际输入仍242662供应商token，applied_job_id为空，因此没有通过应用验收。long-report-r2.json保留失败；账号停用且密钥已撤销。
- 审计确认源分支branch-1e3eaa9e2d5147838e40477a6cd46aa8，正常续聊新分支branch-1e91b0dad5794cf49163072c33ac9feb，relation=continuation且parent指向源分支。后台查询原先只查当前分支，漏掉正常续聊父分支候选。
- 修复只沿服务端确认的continuation祖先查找，最多32级、同lineage、遇分叉/过期/循环停止；候选仍按账号、密钥、模型、协议、完整原消息前缀逐项校验，使用结果仍由当前前台请求写入。20项专项通过，background-lineage.xml，包含多代续聊、修改原文和分叉拒绝。
- 真实HTTP进程测试新增父分支候选应用断言已通过，首轮后续崩溃注入因复用历史被新功能正确压缩、未发起新摘要而失败；修改崩溃夹具首条消息为独立长历史，不改生产逻辑。修正轮session6264，http-background-lineage-r2.xml仍待终态；独立Redis容器8e0a5f1792ee9bfdb7b0117cbc0a238b9ee393822e574bfd79a45a9162d3e3c2须结束后清理。
- 最新全量R8 session35001仍在运行；候选r3已构建image sha256:d4a49e55e4231902dad0e3c3d60248e2836458509a509d7ce47de2341806bad6，尚未部署。R8运行中只调整了可选真实进程测试夹具，生产源码未再变化。

### 生产跨会话找回验收仍在推进

- 首个seed请求b66fec518c1b4a20ad782d49db37128c为503，审计为显式Flash端点unhealthy_or_stale且未选中模型，不是WAL归档问题。临时账号/密钥已关闭。随后现场健康探测确认Flash/Pro均200后才创建下一次独立测试，不盲目重发未知调用。
- 当前独占测试账号context-memory-recall-1ed0d9fd7fc1，只授权自己虚构历史，seed请求2514ac15fcb344e988a0dc85f8e72be4成功。recall_probe.py会话19330保持运行，密钥只在进程内存，最终会撤销并关闭授权；目标报告recall-report-r2.json尚未生成。
- worker首次扫描既有归档并持久化游标，已从3推进到1671，经过API/Control滚动重启未清零。当前chunks仍0，尚未到达本账号新记录，不能把游标推进当成跨会话召回成功。首次全库过滤较慢是当前实际性能边界，需继续等待活跃测试句柄或依据新证据改进，不反复创建账号重置扫描。

### r3最终滚动发布与长上下文应用通过

- 修正后的真实HTTP/进程测试http-background-lineage-r2.xml完整通过；临时Redis 8e0a5f1792ee9bfdb7b0117cbc0a238b9ee393822e574bfd79a45a9162d3e3c2已停止并自动移除。R8全量1875项、1868通过、7跳过、0失败/错误，259.194秒；生产代码在R8运行中未修改，可选进程测试的独立夹具修正另由完整HTTP报告证明。
- rollout_r3.py会话12801退出0，四实例均已更新至r3 image sha256:d4a49e55e4231902dad0e3c3d60248e2836458509a509d7ce47de2341806bad6，逐一排空API/等待实例就绪，restart_count=0；rollout-r3.json保存结果。四实例源码重新核验与worktree一致，仅保留线上registry差异。
- long_probe.py会话79631退出0：长源请求da69be4600bb42cabd984017c4235d21成功，供应商输入242633/输出8。后台任务8accfae378ab42d8aa6c91ae8b03b9b3以2调用、339237估算输入/1774输出、48.04秒进入ready。后续请求f9bca2f9857a47c3ae077af359dfb6cf的审计applied_job_id等于该任务，实际供应商输入仅1673/输出2，ORBIT-003最终端口27003正确，证明正常续聊确实使用后台结果，不只是模型收到完整历史后答对。
- long-report-r3.json记录上述结果，临时账号context-memory-long-073e14501abc已停用、密钥撤销。没有读取个人真实对话作为模型测试数据；供应商usage与本地token估算分别记录，尚未核对最终账单。
- r3重启后的四协议短请求再次全通过，protocol-final-r3.json：Chat非流式679d9787a5af4ad1b0909f37b790efb3、Chat SSE aed39887b68b41d9807f881a7a16091f、Responses非流式a1ec3a3e35164600a0cef853dcbefc0f、Responses SSE 6ba0d3b6caf143909de72094b6a24aff，均200、completed、精确标记匹配；该临时账号/密钥已关闭。
- 跨会话测试会话19330仍在等待首次索引到达来源，游标已超过2369，尚未得到模型召回终态，不重启测试进程或重复创建账号。正式账号尚未开启历史权限；已异步询问用户需要启用的独占Router账号名称与归属确认。
- 当前部署/配置/真实长上下文压缩应用/真实四协议验证已有现场证据，但跨会话召回与正式账号启用仍未完成。无提交、推送或PR，不把上述局部成功标记为整个目标完成。

### Router 客户端账号隔离调整（待发布）

- 用户明确要求所有 Router 客户端账号支持找回自己的历史。授权边界改为认证后的 client_id，同账号多密钥共享账号历史，不同账号不可互查；不再要求账号仅由一个自然人使用。history_owner_confirmed 仅保留为兼容字段，不参与授权。
- 新建托管账号默认开启 history_recall_enabled，云端历史和旧来源授权仍默认关闭；现有明确关闭及旧记录缺失字段均保持关闭，不在读取或普通编辑时暗中开通。local_only 和云端撤权依赖保持生效。
- 管理界面删除个人独占确认，明确显示 Router 账号共享边界。专项 103 项通过；R9 全量 1876 项，1869 通过、7 跳过、0 失败/错误，258.819 秒，报告 full-regression-r9.xml。隔离浏览器 browser-router-account-history-r2 验证桌面/移动端、授权依赖及预算设置通过；git diff --check 通过。
- 线上只读盘点得到 16 个启用的非本轮 context-memory 验收账号（包含仍启用的服务及历史测试账号），历史找回和云端历史授权均未开启。本次尚未批量修改这些账号，也未发布账号策略修订；四实例仍运行 r3。批量开通和新修订滚动发布需确认具体范围，不能把此前部署确认视为新增账号权限变更授权。
- 生产跨会话探针仍沿原账号和游标扫描，当前已超过 6604，chunks 为 0；尚未扫描到测试来源，不宣称正式跨会话验收完成。首次既有归档扫描较慢，批量开启会增加 CPU/磁盘开销。

### 用户确认批量开通后的发布与兼容修复

- 用户回复“继续”，明确覆盖 16 个启用 Router 账号的 history_recall_enabled 开通及 API/Control 四实例滚动发布，不改变云端历史、旧来源或 local_only 权限。原账号快照（不含密钥和用量）及逐项结果保存在 account-activation-r4.json。
- r4 四实例发布完成，image sha256:bf5b39b5eac6bf4df67eb453877b8993569a7f3b0a747fddc52bdb2029dcb788，每实例核验 25 个发布文件哈希、ready、restart_count=0。未替换模型、Redis、LiteLLM 或 Codex Adapter。
- r4 的真实两账号三密钥校验通过：同账号双密钥有效、跨账号拒绝、未授权云端历史拒绝；临时账号停用、密钥撤销。真实四协议请求 94f0b95d2a96436396f9b1d0d167404b、21cc679588c147699bf7ae3c106c17f7、1d105943a2df4d19ac11e622cc653189、9c4ddc5dddd34fffb5633582943b3598 均 200、完整结束、精确标记匹配，新建账号默认值和两入口 models 验证通过。分别记录 isolation-r4.json、protocol-final-r4.json。
- 批量更新只成功开通 1panel，下一项 check-boards 返回 400 后停止。现场原配置含已退役模型 huihui/Qwen3.8-27B-Q4-DFlash2；另一个 workbuddy-ivan-h3 原模型列表为空且 local_only=true。原 PATCH 无论改哪个字段都重新验证整个模型列表，导致只改历史开关也被旧配置挡住。
- 最小修复：模型字段未提交时保留已有退役模型或空列表；新建账号和显式提交 models 仍严格校验。未增加、删除、替换任何账号的模型权限，也未修改 public 身份规则。42 项专项通过，account-stale-model-patch-r4.xml；隔离候选容器用 16 个账号的配置逐一验证 PATCH 全部通过。R10 因补充空列表兼容测试主动中止，不作为成功证据；R11 是最终全量回归。
- 原生产跨会话探针已达到 3600 秒期限，游标 7763、chunks=0，尚未扫描到来源。recall-report-r2.json 记录失败，原测试密钥撤销且账号停用。原因是每账号从头逐条读取/解密归档再筛选归属；没有用手工跳过游标或直接插入索引冒充完整验收。首次索引性能仍待处理，不能宣称跨会话完整验收通过。

### r5 发布、16 账号开通与最终现场读回

- R11 终态退出 0：1878 项，1871 通过、7 跳过、0 失败/错误，261.210 秒；报告 full-regression-r11.xml。界面 Node 检查和 git diff --check 通过。R11 运行期间未修改生产源码或测试，后续仅补充交付文档。
- r5 四实例完成滚动更新，image sha256:78c88072a730da18f1cd7723e077f281c47fdf3dbc2894516e5da1cc9a7a13d1；每实例 25 个发布文件哈希与工作树一致，ready、restart_count=0，两 API draining=false。报告 rollout-r5.json、verify-r5.json。模型、Redis、LiteLLM、Codex Adapter 和 Fleet Adapter 未重启。
- 先核对 r4 已成功的 1panel 状态，再仅 PATCH 剩余 15 个账号的 history_recall_enabled=true。16 个目标全部完成，两 Control 入口读回一致；云端、旧来源、local_only、模型列表、限额及其他账号配置均未改变。account-activation-r5.json 的 verified=true、updated_count=16；原快照保留，无密钥导出。
- 最终部署上的两账号三密钥验证再次通过：同账号可用、跨账号拒绝、未授权云端历史拒绝，isolation-r5.json。真实 Chat 非流式 dbcd60a838564a2cb053f5c296c1c383、Chat SSE 161c9fbfbb4740b4a41b6872d6078075、Responses 非流式 2492fb27c0c949d88ee3333de75c979e、Responses SSE 51b841dc6f7e4735a45f2db26ba7b5ad 均 200、完整结束、精确标记匹配，protocol-final-r5.json。新账号默认召回开启/云端关闭及两 API models 通过。所有本轮临时账号停用、密钥撤销。
- account-index-status-r5.json 显示 16 个账号游标均开始推进；1panel 已有 136 会话、5267 块，游标 1616；其余账号游标 21–55、尚无块。这只证明索引工作和账号开通，不代表所有旧历史已可召回；尤其真实跨会话模型找回此前因首次扫描超时仍未通过，不以授权边界或四协议成功替代该验收。
- 当前账号能力发布及批量开通已完成，但首次索引性能和完整跨会话验收尚未完成，因此仍无提交、推送或 PR。所有改动留在独立工作树，原分支未合并。

### 共享读取与增量优先改造（本地候选，尚未发布）

- 用户确认继续完成“减少重复读取、新记录优先、限额补旧历史、暂停续跑和可见进度”的方案。本轮只改独立工作树和隔离测试，没有写生产设置、账号或数据库，没有构建/替换生产镜像，没有模型调用。
- HistoryMemory 从逐账号全库扫描改为全局租约下共享读取，每条变更校验密文与 request_hash 后按所属账号投递；索引的账号 HMAC 命名空间、来源核验、排除列表、云端授权边界不变。普通代码执行，不增加 Agent 或模型管理层。
- 新增可重建索引库内 memory_ingestion 进度表，分别保存旧历史边界/游标、新增变更游标；初始化时以当时归档末尾划定边界，之后的新变更优先于旧历史。首次启动前已经存在的记录仍属于待补历史，不能承诺这些记录立即可用。中途新开通账号可独立补历史，可能重新读取此前已经为其他账号扫过的区间；“一次读取”指同批活跃账号共享处理，不是跨所有故障重放和后续授权永远只读一次。
- 继续维护旧 memory_checkpoints 的连续完成游标。未补齐旧区间前不把它推进到新增记录之后，重启不跳过空洞；重新运行旧版产生的已验证连续游标也会被新版本接纳。写入/租约失败不推进该批游标，重放依赖原有文档去重，不删原文、不手工跳过失败来源。
- compaction.history_indexing 默认 enabled=true、batch_records=8、max_batch_seconds=0.25、duty_cycle=0.2；每轮先一批新增、再一批旧历史，工作后持有全局租约休息，避免两个 API 各用一份时间占比。无任务时约 5 秒检查一次，无解密旧记录。限额是协作式工作/休息时间比例，不是 cgroup CPU/内存硬限制；单条大记录可能超时限，必须完整处理，不为满足预算截断。
- 全局暂停只停止索引维护，不撤销账号授权、不删除已有索引。后台按设置文件标识变化加载索引参数，无前台聊天也可暂停/恢复。管理界面显示待处理变更量、最近墙钟处理速度与粗估剩余分钟；首轮无采样、或速率超过 60 秒未更新时不提供 ETA。序列进度计的是归档变更，不是唯一会话数或固定大小字节。
- 合成基准 shared-index-benchmark-r1：16 账号/128 条记录，0 模型调用，最终来源 ID 集合完全一致。旧逐账号算法重现使用当前共同 SQLite/index 实现，不是旧容器二进制；不限速 7.037 秒、CPU 6.185 秒、2048 次解密；共享不限速 2.710 秒、CPU 2.559 秒、128 次解密，约 2.60 倍墙钟提升、解密次数减少 16 倍。共享默认限速 16.478 秒、CPU 3.211 秒；低资源优先会延长完工时间，不把不限速成绩换算成生产承诺。
- 首轮 71 项专项及 Node 界面测试通过；browser-shared-history-index-r1 通过桌面/移动端、暂停与参数上下限、进度/ETA 渲染，截图可追溯。shared-index-real-redis-http-r1 的 3 项真实 Redis/真实 API+Control 进程测试通过，包含重启后检索结果注入四协议及后台压缩故障恢复，模型为纯 loopback 假服务，不是线上模型质量验收。
- 后续补充无前台请求的设置重载、统一来源验证函数、实际 HTTP 暂停/恢复验证；最终全量 R13 与真实进程 r2 结果另记。上线前需要备份当前索引库及设置、确认新进度表及四实例发布范围；原始归档库不增加表、不迁移原文，原文和账号权限保留。

### 边界复核与冻结候选

- 增加共享新增游标 memory_ingestion_meta。账号停用后重新开启时，把它错过的旧新增区间转入自己的待补历史，不让该区间挡住其他账号的新记录；新开通账号也不能把其他账号尚未处理的新记录挪回旧历史。重组进度时清除旧速率采样，避免虚报处理速度；接纳旧版连续游标后同步旧格式检查点。相关专项共 77 项通过（shared-index-targeted-r3.xml）；随后补充检查点/采样边界纳入最终 R15。
- HTTP r2 的暂停测试过早以“游标动了一点”为验收条件，修正为达到测试来源对应的精确变更边界；r3 已通过暂停/恢复找回，但后续共用账号的压缩故障注入未发起摘要。连续源请求/摘要/续聊的估算输入可能在同一自然分钟超过 100K，测试也不应共享历史状态。故障注入改用独立合成账号，保留相同 100K TPM 和并发限制，没有修改生产限流器或账号预算。HTTP r4 三项完整通过；最终冻结后另跑 r5。
- 最终基准 shared-index-benchmark-r3：旧算法重现 6.852 秒、CPU 5.950 秒、2048 次解密；共享不限速 2.564 秒、CPU 2.449 秒、128 次解密，墙钟约 2.67 倍、结果来源集合完全一致；默认限速 17.427 秒、CPU 3.402 秒。结果仅针对 16 账号/128 条合成记录，模型调用为 0，不外推真实 12GB 归档的完成时间。
- 冻结范围 ai_router/config/tests/scripts 的文件内容汇总 SHA256：7ecca0bae06b3ed684b9fef9a46586780812beeeeb62e1eafc8eec59eea8df32。R14 因补齐最后的旧版本进度及采样边界主动中止，不作为通过证据；R15 是最终生产源码全量回归。冻结后只更新本文档，不修改源码或测试。
- 仍有明确资源与恢复边界：首次启用之前的记录归入旧历史；新开账号、重新授权或失败重放可能重复读部分区间；单条大记录不可中途截断，可能超过单批目标并延长冷却等待；损坏来源失败关闭、不跳过；硬崩溃后其他 worker 最长可能等待原 120 秒租约过期，普通有序重启会释放租约。已有索引和原文不删除。
- 本轮生产仍为 r5。发布候选需要在线备份 history-memory.sqlite3 和现有设置，在该可重建索引库新增两张进度表，保留归档库与账号授权，按默认 20% 协作式工作时间占比配置，再滚动替换 API/Control 四实例；必须把新增 memory_ingestion.py 纳入部署镜像。本轮尚未进行这些生产操作。

### 冻结候选最终验证结果

- R15 退出 0：1901 项、1894 通过、7 跳过、0 失败/错误，266.114 秒。结果 full-regression-r15.xml。结束时源码/配置/测试/脚本汇总 SHA256 仍为 7ecca0bae06b3ed684b9fef9a46586780812beeeeb62e1eafc8eec59eea8df32，与冻结值一致；git diff --check 和 Node 界面检查通过。
- shared-index-real-redis-http-r5.xml：3 项、全部通过、121.163 秒，覆盖真实 Redis 租约互斥/续租、两个实例的进度恢复，以及真实 API/Control 进程的四协议找回注入、暂停期间不前进、无前台请求恢复后处理到精确来源边界、正常重启、后台压缩应用与硬崩溃后未知结果处理。模型是隔离 loopback 假服务；不声称已完成线上模型验证。全量中的 7 个跳过项没有混算为通过，其中本轮相关的上述 3 项另行实跑。
- 隔离 Redis 容器 context-index-redis-20260913-r1（ecf8115a7f0bdd830985993daf8bfe39fa5848ccf55c88eec4d3b7a7fb021306）已停止并自动移除，临时 Redis 数据随实例释放；测试报告保留。端口 14873 的 UI 预览已正常停止。真实进程测试的子进程由 finally 清理。
- 四个生产 Router 容器现场仍显示 context-memory-20260913-r5，本轮没有更改生产数据库、设置、账号或镜像，也没有提交/推送/PR。代码与隔离验收已完成；下一步待确认备份、新进度表及四实例发布后，再做真实线上验收。

### r6 用户确认后的生产发布与真实找回验收

- 用户明确确认索引/设置备份、可重建索引新增两张进度表、API/Control 四实例滚动发布及默认 20% 工作时间占比。本轮未改原始归档结构或现有账号权限，未重启模型、Redis、LiteLLM、Codex Adapter、Fleet Adapter。无提交、推送、PR 或分支合并。
- 发布前源码/配置/测试/脚本汇总 SHA256 与 R15 冻结值一致。备份 `/training/backups/context-memory-20260913-r6/history-memory.sqlite3` 为 6564225024 字节，SQLite quick_check=ok，同目录保留 settings.yaml；备份完成前通过既有全局 worker 租约暂缓索引写入，完成后释放，未删除或抢占其他 worker 锁，不暂停前台聊天。报告 backup-r6.json。
- 镜像 `1panel-ai-router:context-memory-20260913-r6`，ID `sha256:e9f54045d18a3cf5fc634d96eef454ae58241644af9094dd7c7d8c3228439e59`。沿用原 Compose overlay 链，逐实例确认环境、命令、网络和挂载不变，两 API 均排空到 active_request_count=0 后替换。四实例 ready、restart_count=0，每实例 26 个发布文件哈希与工作树一致，两 API draining=false；报告 rollout-r6.json、verify-r6.json。
- 生产 registry.yaml 与 r5 的 SHA256 均为 `026b11d3e2abeec89825281eff6747a3e780c545f36a92a080a069a625f83db4`，没有用工作树中的保守上下文配置覆盖线上注册表。保留 r5 镜像及 compose.rollback-r5.json 作为上一版本回退候选；回退同样需要排空且追加该 overlay 到链末，不应恢复整个旧原文库或随意删除进度表。
- 索引库从四张表增为六张，仅新增 memory_ingestion、memory_ingestion_meta。account-index-status-r6-before.json / account-index-status-r6.json 核验 16 个现有账号 chunks 与连续游标均不倒退，均有新进度；history_recall_enabled=true、cloud/legacy=false、local_only 原值保留。旧历史仍在后台补齐，本次成功不表示全部历史已经可找回。
- 两 Control 读回新配置一致：enabled=true、batch_records=8、max_batch_seconds=0.25、duty_cycle=0.2；其余设置保持原值，压缩模型仍 cloud-deepseek-v4-flash，上下文模式仍 compact，扩展参数仍 500000。settings-r6.json。20% 是协作式工作/休息时间比例，不是整机 CPU 硬限制。
- 真实新记录优先及跨会话模型找回通过，recall-report-r6.json。独立合成账号先初始化进度，再发源请求 `7614c6406d59478091070bcaff576cbe`（200，3.609 秒，供应商输入 39/输出 1）；新记录约 20.058 秒后已有索引，此时该账号旧历史连续游标仅 18、仍有约 1.4 万条变更待补，证明没有等待完整回填。
- 随后全新会话请求 `3f6312b2d2b14d588bf6b9e491753606`（200，2.173 秒，供应商输入 240/输出 2）只问唯一服务标识，未携带端口或上一轮消息，正确返回 7241。trace.history_recall.state=injected、added_tokens=263，来源 `14798d3b730a6c13509c570a1846da756ab213d6956b081e8a80dcc302d8c17e` 读回确认归属该测试账号且 request_id 对应源请求。Router 附加 token 估算与供应商计数分别记录，不混为相同口径。该次 20 秒为单次观测，不是时延保证。
- 真实四协议全部 200、完整结束、精确标记匹配，protocol-final-r6.json：Chat 非流式 `d38d180e88594957b9e9130707c86f14`，Chat SSE `4230073075f74e01b3b90a0ff1e4e75b`，Responses 非流式 `e89dba05361d4b9aa2bd27b7e728ced0`，Responses SSE `1c9d604b8ee0496d9865ebfec4641066`；两入口认证 models 与新账号默认值亦通过。isolation-r6.json 验证同账号双密钥有效、跨账号拒绝、默认云端历史拒绝。
- pause-resume-r6.json：线上暂停后等待 worker 释放租约，连续 12 秒索引游标完全不变；恢复后未发送前台 API 请求即观察到进度继续，全部设置精确恢复，最终 enabled=true。没有借暂停删除索引或撤销账号授权。
- 所有本轮临时账号停用、密钥撤销；仅合成找回账号临时允许它自己的虚构历史发送到云端，现有 16 账号云端历史权限未改变。索引构建本身不调用模型；压缩摘要仍按独立模型设置运行。最终源码汇总 SHA256 仍为 R15 冻结值，git diff --check 通过。证据目录 `/home/ai/.local/state/ai-router-acceptance/20260913-context-memory`。

### 审查后 R1/R2 修复（独立工作树，未发布）

- R1：后台 worker 检测共享设置变更，在领取任务、心跳和模型派发前刷新检查；关闭后不再派发新调用，已派发且结果未知的任务不盲目重试。
- R2：前台、自动后台和 Control 手动整理复用一套来源证明及滚动摘要逻辑。已证实的 Router 摘要合并为一份，用户原始 system/developer 指令不变；原消息 API/加密会话结构兼容，无新会话控制层。
- 旧会话按需从同账号、服务端已验证延续链的加密 ready 任务恢复来源。没有可靠来源记录的旧摘要继续保护，不按文本前缀猜测、不批量重压缩；若受保护内容已装不下，明确报错而非删除或扩大上限。
- 40 轮固定摘要结构探针通过；两种协议的连续压缩、账号/分支/协议/前缀隔离、存储故障和旧任务迁移测试通过。最终全量 1907 通过、8 跳过、0 失败；额外真实 API/Control/Redis 恢复测试 4 项通过，具体执行先后见审查记录。
- Flash/Pro 各 5 轮真实合成长输入压缩（约 33.9 万 token），总计 20 次模型响应，均只有一份 Router 摘要。第 5 轮 40 个服务最终端口问答两者均 40/40；原始权限规则与工具路径/配对另由机械断言验证，不与模型问答评分混淆。
- 证据目录 `/home/ai/github/1Panel-worktrees/context-validation-dxsqjn`，文件前缀 `rolling-summary-`；最终源清单 SHA256 `131e34447f28c1d7e60ad2a334cfd78cde3f55d53105411a9ffa5c246c2a5571`。细节与限制见 `context-memory-code-review-20260913.md`。
- 本轮没有修改生产设置/账号，没有重启生产服务，没有 Git 提交、推送或 PR。部署需要再次确认范围并纳入新文件 `summary_provenance.py`，随后验收最终生产协议与设置开关，再按约定创建 PR。
