# AGX Cerebellum 参数对照

本轮按用户批准的顺序测试纯文本运行参数，不改内核、PCIe、电源设置或原生产 unit。
保留 Q3_K_M、262144 上下文、Q8 K/V、单槽和全层 GPU 卸载。
生产路由始终保持原先的停用状态，不自动部署测试候选。

## 工件与脚本

- 独立修复后端：
  `/data/agx-runtimes/cerebellum-mtp-20260904/exit-fix-v1/source/build-agx-cuda/bin/llama-server`。
- SHA256：`830987adaaa4de9e52389386121206fb013a11a1c0c09280c859ee55c16cb5e4`。
- 历史合并工件、退出问题及构建细节见 `agx-cerebellum-mtp.md`。
- `validate-agx-mtp-quick.py --parameter-screen --text-matrix` 串行运行下面四组。
- 非 MTP 候选同样回放基线 fixture，不重新生成自己的会话历史。
- 同时检查固定长度计时、正常 EOS 文本/工具、三位置精确检索、正常退出和原服务恢复。
- `--runtime-profile` 可将同一资源配置应用到后续 off/MTP1 两档。
- `guard-agx-translation.py` 暂时阻止翻译自动唤醒，不重启共用 TTS 网关；
  有独立恢复定时器，所有测试结束后须撤销。

## 第一阶段：4K 短测

证据：`/home/ai/.local/state/ai-router-acceptance/20260905-agx-parameters/short-screen/`。
两类负载为分析和代码，各四轮，每轮计时固定生成 256 tokens。
温度 1、top_p 0.95、top_k 20，候选请求哈希逐轮匹配基线。
计时用例忽略 EOS，独立正确性用例不忽略 EOS。

| 组别 | 历史缓存 MiB | 检查点上限 | batch/ubatch | 生成/预填充线程 |
| --- | --- | --- | --- | --- |
| baseline | 8192 | 32 | 2048/512 | 12/12 |
| cache | 4096 | 8 | 2048/512 | 12/12 |
| batch | 4096 | 8 | 1024/256 | 12/12 |
| threads | 4096 | 8 | 2048/512 | 6/8 |

四组的正常文本、工具、三位置检索和退出检查通过。
原模型恢复后 7 项检查通过，维护耗时 627.336 秒。
此恢复状态不代表跨阶段的翻译隔离已撤销，后续须单独记录。

| 组别 | 分析暖轮 TPS | 代码暖轮 TPS | 首轮预填充，分析/代码 |
| --- | --- | --- | --- |
| baseline | 34.73 / 34.59 / 34.44 | 6.82 / 34.30 / 34.48 | 8.420 / 8.417s |
| cache | 34.87 / 34.71 / 34.64 | 34.80 / 34.77 / 34.57 | 8.372 / 8.413s |
| batch | 34.82 / 34.73 / 34.62 | 34.85 / 34.75 / 34.65 | 12.024 / 12.049s |
| threads | 34.97 / 34.76 / 34.57 | 34.90 / 34.65 / 34.54 | 8.363 / 8.391s |

基线代码第二轮异常：request ID `84621af0a4d34c1cb252124c8fc7452a`。
缓存复用 4073 tokens、新预填充 282 tokens，预填充只耗时 0.763 秒；
但 256 tokens 解码耗时 37.536 秒，比相邻正常轮约多 30 秒。
对应时间窗口出现 NVMe timeout 日志，证明存在运行停顿，但没有建立完整底层因果链。
保留原始结果，不能将这一轮制造的约 5 倍表面提升当成调参收益。

因此额外使用稳定 cache 组的全部六个暖轮作为参照，文件
`stable-reference-comparison.json`：

- batch 解码配对中位变化 +0.016%，总耗时 +1.756%。
- threads 解码配对中位变化 +0.026%，总耗时 -0.058%。
- 当前样本不支持批量或线程调整能明显提高吞吐的结论。

batch 的 GPU 工作区从 812.28 降至 406.27 MiB，
主机工作区从 520.29 降至 260.28 MiB；这是明确的空间/预填充速度取舍。
四组均没有触发检查点容量淘汰，短测中的实占内存也接近。
不能把缓存上限降低、检查点上限降低等同于已经释放相应容量或已通过长会话验收。

后续优先保留 2048/512 和 12/12，在 cache 资源配置下独立验证 MTP1。
先验证完整推理后的正常退出，再逐步增加实际输入长度。

## 第二阶段：修复后端 MTP1 短测

证据：`/home/ai/.local/state/ai-router-acceptance/20260905-agx-parameters/mtp1-short/`。
两档均使用 cache 配置、纯文本、256K 配置上下文，实际输入约 4K；
不能将此结果称为实际 256K 输入验收。

| 组别 | 分析暖轮 TPS | 代码暖轮 TPS |
| --- | --- | --- |
| off | 34.87 / 34.76 / 34.54 | 34.81 / 34.59 / 34.50 |
| MTP1 | 38.55 / 40.12 / 40.83 | 39.70 / 37.61 / 37.55 |

相同 fixture 的六个暖轮配对结果：
解码速度中位提升 12.297%，请求总耗时比为 0.911435，即减少 8.857%。
正常 EOS 文本、工具及分析/代码三位置检索均通过。
性能汇总保存在 `comparison-mtp1-recovered.json`；
比较有效，但长输入性能门槛和部署批准仍为 false。

原始 `report.json` 的整体 `passed=false` 必须保留：
停止测试 unit 的 SSH 命令超过脚本的 30 秒等待上限，打断了退出证据收集和汇总。
这不等同于推理失败，也不等同于完整测试通过。
人工复核 unit `agx-cerebellum-mtp-quick-f8d3afc2-mtp1.service`：

- AGX 本地时间 2026-09-05 00:27:10 开始清理并注销 MTP draft head。
- 00:27:43 systemd 记录 `Deactivated successfully`，未再出现 double-free。
- 退出状态为 `Result=success`、`MainPID=0`、`ActiveState=inactive`、
  `ExecMainCode=0`、`ExecMainStatus=0`。
- 同一秒有 NVMe completion-polled timeout，加载阶段也有三次。
  这是关联证据，不足以确定 33 秒清理耗时的完整因果关系，也不能宣称硬件问题已解决。

测试脚本停止等待改为 120 秒，并增加回归测试；没有修改 systemd 的停止策略。
四个相关测试文件共 60 项通过。新等待逻辑尚未重新跑完整实机矩阵。

## 收尾与结论边界

本轮原服务恢复成功，7 项文本、工具及图像检查通过；维护耗时 564.799 秒。
原生产 unit 未替换，路由保持 `enabled=false`，没有上线 MTP 候选。
翻译隔离已释放，临时 drop-in 已移除，恢复定时器已取消。
翻译实请求返回“翻译服务已恢复。”，
request ID 为 `chatcmpl-VtajfOr2F9TiIRSbWAVj73u22GOGV0Bw`。
共用 TTS/翻译网关 PID 仍为 2106714，未重启。

当前优先候选是 MTP1，保留 batch/ubatch 2048/512、线程 12/12。
4096 MiB 历史缓存和 8 个检查点仍属于待长会话验证的资源配置。
本轮没有追加 42K、128K 或接近 256K 的实际输入测试；
不能把短测结果或历史未修复后端结果直接外推为新配置的长上下文结论。
