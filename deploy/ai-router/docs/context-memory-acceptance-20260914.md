# 上下文管理与历史找回：本次 PR 验收摘要

## 提交范围

- 保留既有上下文管理，提供压缩与扩大上下文两条可配置策略。
- 结构化分段摘要、工具事务保护、来源校验、后台加密任务与检查点、预算和故障恢复。
- 按已认证 Router 客户端账号隔离历史索引、查询和原文找回，调用前复核授权。
- 设置界面及摘要生成参数；拒绝中断输出，修复发送前设置变更与预算回收。
- 不包含主工作目录的 H3、注册表调优等并行修改，也不包含仓库外独立安装的 Flash 审查工具。

## 已有验证

以下为冻结候选 r15 的实测结果，不是本次提交时重新运行全量测试。

| 验证 | 结果与边界 |
| --- | --- |
| 定向回归 | 181 通过，0 失败/错误/跳过 |
| 运行环境全量回归 | 2037 通过，0 失败/错误/跳过，448.510 秒；生产派生依赖、真实隔离 Redis |
| 双 API / 双 Control | 16 个协议和账号场景通过 |
| 短稳定性 | 109.322 秒、8 阶段；180 个正常/恢复请求零错误，24 个故障请求均为预期 429；结束无活动请求及租约 |
| 发送前配置竞争 | 8 种时机覆盖；未发送不计为未知结果，回收调用额度并释放预算预留 |
| 中断摘要 | 2 种中断结束状态拒绝写为成功检查点 |
| Flash 正式参数单点补测 | low + 30000，HTTP 200 / stop，35.727 秒，结构及三个关键标识校验通过 |

Flash 补测于 2026-09-14 单独授权，不计入前一天的 15 分钟限时验收；操作 ID 为 `26c6b36aacdb4922831951aac112d1ed`，供应商响应 ID 为 `c773647e-35d5-4b95-ac81-05fe16592212`。实际输出 7609 token（思考 6866），低于旧 8192 上限，因此一次成功不能证明提高上限是唯一原因。

证据目录（本机，原始产物不随 Git 发布）：`/home/ai/github/1Panel-worktrees/context-validation-dxsqjn/acceptance-20260913-IQqEL7`。主要文件为 `profile-targeted-r15.xml`、`runtime-regression-r15.xml`、`soak-smoke-r15/soak-report.json`、`flash-failure-probe-r15b/report.json`。

冻结源码 SHA256：`30fc3270c648875b059e22891e956bc033190d427730bac427d4cdee8333529e`。提交前复核其 510 个文件哈希一致；随后仅新增本摘要及更新交付文档入口，不修改已验证的运行代码。

## 明确未完成的事项

- 用户取消的两小时稳定性测试、完整两模型 160 题最终质量重测没有完成；短测试不能替代长期稳定性证明。
- 尚未实现根据真实缓存命中和成本比较来延后压缩的策略。目前后台预压缩仍按上下文占用阈值判断，不能宣称“命中缓存就不压缩”。
- 未实现缓存保留与重新摘要的自动成本比较、基于实测噪声或延迟的压缩触发、摘要前缀稳定性优化。
- 摘要输出默认仍为 8192，思考默认 provider_default；测试配置不等于生产默认。
- 创建本 PR 不代表合并或部署。本次提交/PR 操作不修改生产配置、不重启服务。

历史实施过程与阶段失败见 [交付记录](context-memory-delivery.md)，其中较早的“尚未完成”状态按时间理解，最新验收范围以本页为准。
