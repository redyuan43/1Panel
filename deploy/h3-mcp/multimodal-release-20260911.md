# H3 多素材接入：实现、验收和待发布状态

## 当前结论

### 2026-09-11 用户授权后的部署进展

用户已明确授权开始部署，并告知 WorkBuddy 已启动。本次重新核对 Windows SSH 在线、旧 H3 HTTP 配置具备专用凭证、本地桥接组件尚未安装；Studio/Fleet 当前没有运行或排队任务，两 Control 基础镜像与候选一致。
AI 持久备份与不可变候选已准备：`/home/ai/.local/state/h3-studio-ivan-production/deployment/multimodal-20260911-r1`。包含一致性数据库备份、项目素材副本及摘要、全部 Compose 覆盖、容器身份和私密配置。
Fleet 已切至 `/mnt/ivan-ext4-offload/h3-fleet-releases/20260911-r5-2a477c833431`，Studio 已切至 `releases/20260911-multimodal-r1`，两个 Control 已切至镜像 `sha256:e3c103ee9a7043eeb2083546a684f97624b099c1154b55b53f5ed0494de96b1d`。三个控制面健康；新素材页面、后台状态和设置接口200，匿名管理请求401。六个 Ivan worker 身份、66条 Fleet 历史及19个无关容器身份保持不变。
部署维护门禁已解除，Studio与两Control的生成环境上限和管理策略均为启用，Fleet后台已启用。四配方沿用原资格开放4060 Ti单路；最后只读采样A4/B8各显示1个兼容槽位（同一GPU，不能相加），Swap恢复连续稳定120秒，运行/排队均0。多素材配置仍未通过执行资格，不以开关绕过。

- Windows 本地组件已安装，原MCP配置和Skill有私密备份；配置中不再保存内联凭证。安装版本 `a98580bcc6132e255b34cd04571184c99e45355e93adab4dc39b68fa97cee119`，回滚回执位于 `%USERPROFILE%\.workbuddy\h3-bridge\installation.json`。
- Windows独立stdio验收完成初始化及11工具发现；`windows-11-tools.json`明确不是WorkBuddy界面自然语言验收。WorkBuddy自身曾启动新bridge进程，其父链已核实；尚无客户端自身11工具发现的完整日志证据，不能把脚本结果替代它。
- 原照片通过Windows已安装组件上传成功：`asset_1ffde8cf39a84559994d1c874df95370`，328682字节，383×680，摘要与原文件完全一致，上传回执复查一致。
- 任务`7732e9926474`现已通过版本化接口新增首帧输入，提示词和64位种子保持原值；旧T2V文本、批准和输出保留在历史，旧审批不再有效。新版本`b5dc29150361dced97b6f28c7fafdd966b85902cd82d47e32ae01dd80f343650`使用`H3_I2V_QUALITY14`，状态`awaiting_prompt_approval`，已向用户请求对这一具体版本重新确认，没有自动开始生成。
- 真实网页只读验收：首页、原任务新首帧页、设置页均通过，无JavaScript错误、无写请求；原图完整解码显示，使用contain，不裁掉下半幅。截图和回执在AI部署目录，`browser-smoke.json`为最终记录。中间验收脚本先误用未安装的Chromium包装器、后使用了旧开关按钮文字；已修正为现有Chrome和真实按钮状态，没有为测试安装浏览器或修改生产行为。
- Windows工具发现脚本最初使用系统GBK解码UTF-8结果而报错；改为显式UTF-8后11工具发现通过。此错误来自验收脚本，不作为客户端连接失败证据。

**尚未完成：** 新首帧版本的人工确认、Ivan素材准备/真实首帧成片/播放与资源释放验收、其他多素材分支执行证据，以及WorkBuddy界面自然语言端到端留证。本次没有新增GPU推理；已部署不等于六模式全部生成通过。
下文“未获得授权/Windows离线”是授权前的历史状态，不再作为本次阻断。
REF2VA 下载句柄已返回 `download_integrity_failure`，保留临时文件与回执，不注册不合格权重，不盲目重试；不阻塞已有 FL2VA 权重的部署。

### 授权前的候选状态

主要代码已形成离线候选；**未部署、未更新实际 WorkBuddy 安装、没有新增 GPU 推理**。已补齐参考视频实际帧率检查与任务级解码预算；非恒定24fps输入明确拒绝执行，不静默变速或假称已转换。仍不能把离线代码称为完整端到端交付。
维护窗口已请求确认，不能把下列测试通过写成六模式已上线或真实成片通过。
授权前原任务 `7732e9926474` 的文本、批准、种子、历史和产物未修改；授权后的版本化首帧编辑见本报告顶部。

## 已实现

- Studio 统一保存账号所属素材、上传回执、原文、创作来源和输入版本；图片/音视频实际解码校验、限制大小、配额、超时及过期临时文件清理。
- 六模式和合法音频组合均有专用工作流绑定检查；任一素材未连接输出、摘要不一致或配置不匹配即拒绝提交。T2V 仍只有四配方；多素材使用 FL2VA/REF2VA 完整模型14步。
- 审核绑定文字、素材用途/摘要、模型配置、音轨、种子及规格。编辑输入生成新版本，不篡改旧批准/旧产物；网页与 MCP 使用同一业务契约。
- 本地受限 stdio 接入组件转发九个 MCP 工具，增加 `h3_upload_asset` / `h3_get_upload`。只接受附件目录内明确媒体或用户通过本机选择器指定的文件，禁止任意 URL、Shell、扫描和凭证输出。
- 安装器校验原配置摘要，保留其他 MCP，使用受保护的本机凭证文件；先备份和写安装回执，再切换。失败自动恢复自身修改，遇到并行用户修改则保留并报告冲突，不覆盖。
- 本机私密 Token 配置器已打包；只做认证与工具发现，不调用容量或生成接口，不跟随携带凭证的重定向。并发配置窗口互斥，失败保留旧凭证。设置页区分本地素材组件与远程 HTTP 向导，避免重新授权时把 stdio 上传入口覆盖掉。
- 勾选视频原声时必须绑定参考视频；视频原声和锁定音频都要求已实际解码验证的音轨。不能接受设置后再静默丢弃音轨。
- Fleet 唯一派发入口；模型启动异步进行，保存同一执行编号。服务身份、运行版本、GPU UUID、实际输入目录及服务定义均校验；加载状态不由进程在线冒充。
- 后台停用保留排队任务，健康任务结束后受控卸载/停止；300秒空闲卸载沿用原逻辑。启动失败/结果未知隔离，不无限重启。借用 Ivan 已有非交互权限，只调用登记 H3 服务的 start/stop，不增加宽泛系统管理 API。
- 设置页分开管理新生成接单与后台启用；任务页展示输入缩略图、用途、配置、阶段状态/耗时，完整画幅预览，历史切换不复用旧图，原64位种子编辑时不经 JS 浮点数改写。

## 不放宽保护

- 保留72GiB准入、原运行期硬限制、Swap/PSI、主机可用内存、显存、磁盘和至少60秒稳定观察。
- 多素材候选预算不低于原长任务24GiB，较高历史峰值预算不降低；冷启动另计保守预算。不提高 cgroup 限额，不 drop_caches、swapoff 或全局 reclaim。
- 正式多素材执行范围暂只允许与本轮验收一致的15秒竖版480×864、24fps；其他规格可保存草稿但不能凭一次证据宣布全部可运行，也不自动改规格。
- 新配置默认 `not_validated`。临时 `designated_acceptance` 必须同时具备指定任务前缀、未过期窗口、Fleet 发布隔离门禁及全局单路；不会显示为正式通过，不授予混跑资格。关闭隔离门禁或到期自动失效。

### 参考视频解码预算

- 实际解码逐帧读取 PTS、尺寸、像素格式、帧数和所有音轨信息，不拿平均帧率或输出480×864替代真实输入。实际恒定24fps原件可以直接绑定；非24fps/VFR保留原件及草稿，启动前明确拒绝。当前没有自动生成分析副本的能力，不伪造副本摘要。使用视频原声还要求唯一音轨。
- 令 `F = 实际输入宽 × 高 × 4通道 × 4字节`，保守解码估算 `D = 2 × 实际帧数 × F + 32 × F + 音轨双份float32缓冲 + max(2GiB, 前述小计的10%)`。这是分配结构估算，不是实测完整峰值或绝对安全证明；真实峰值历史、全局保护仍独立保留。
- 任务预算为 `max(保留的历史总预算及实测峰值余量, 模型基础预算 + D)`。模型基础预算至少24GiB，旧记录缺少分解时按保守基础下限继承；新记录分别保存基础和输入增量，避免下一条把同一解码预算重复叠加。
- 入队 demand、候选选择、事务内复验、旧热模型清理以及冷启动都带同一任务的可信输入证据。冷启动另加原有预算，运行任务剩余峰值预留不变。4K整段参考解码可能超过整机准入线，不能因输出是低清就放行。
- Fleet 在核对原件SHA后自己探测，覆盖客户端回传的预算字段。只使用受互斥保护的64项进程内缓存，命中前验证内容和文件身份，返回深拷贝；不相信可编辑的磁盘metadata缓存，重启后重新探测。素材准备使用有界CPU子进程：2GiB虚拟地址空间、每进程90秒CPU及整次90秒墙钟上限，限制输出并回收超时进程。两台服务主机已只读确认具备 `/usr/bin/prlimit`；没有修改系统全局限额。

## 离线证据

| 验证 | 结果 | 证据 |
|---|---|---|
| Studio/Control/MCP 相关完整回归 | 654通过，0失败/错误/跳过，46.08秒（06:44 历史结果；口径为 h3-mcp 侧 438 + ai-router 侧 `tests.test_h3_*` 216，与下文的复跑数字不是同一口径） | `/tmp/h3-multimodal-full-r12.xml` |
| 私密配置器专项（已包含在完整回归内，不重复计数） | 7通过；Windows 实机分支未验证 | `/tmp/h3-private-configuration-final.xml` |
| 新安装/资格/生命周期保护测试 | 22通过 | `/tmp/h3-multimodal-release-guards.xml` |
| Fleet回归及4项实际调度方法集成测试 | 1194通过，11个既有失败；无新增失败，27.90秒 | `/tmp/h3-fleet-full-r7.xml`、`/tmp/h3-fleet-baseline-old-tests.xml` |
| 参考视频CPU探测/预算专项（已包含在完整回归内） | 73通过，含真实24/30fps、VFR、极短4K合成素材、缓存与进程上限 | `/tmp/h3-reference-memory-final-focused.xml` |
| Node 接入组件 | 5通过 | `tests/bridge.test.cjs`，Node22实测 |
| 浏览器 | 素材/历史/完整画幅/参考帧率9项、设置12项、输入编辑7项通过 | `tests/browser_input_assets.cjs`、`tests/browser_mcp_settings.cjs`、`tests/browser_input_edit.cjs` |
| Control 候选镜像 | 无生产挂载、无网络/无GPU容器导入及依赖检查通过 | `sha256:e3c103ee9a7043eeb2083546a684f97624b099c1154b55b53f5ed0494de96b1d` |
| 候选完整性 | Studio64、Fleet46、Control6、WorkBuddy5个文件摘要全部匹配 | 对各自 `candidate-manifest.json` 逐文件复核 |

Fleet 既有失败：旧归档文件清单不一致1项，以及旧隔离实验测试使用 `/tmp` 输出被现有持久路径保护拒绝10项。不修改这些无关保护，也不把全量回归称为全绿。
测试中的合成图片/模拟视频仅证明契约、权限、幂等及流程，不是实际成片或画质证据。
测试候选附加的旧实验文件仅为测试夹具，**不得随正式 Fleet 归档发布**。
限定复核曾指出磁盘探测缓存缺少独立可信性、旧模型清理路径漏传任务预算两项；已修复并重新核对源文件/冻结摘要。最终全回归保留旧卸载测试及新增传参检查，不能仅以源码字符串检查代替运行验证。中间一次回归因遗漏旧实验夹具而出现14项收集错误，已在隔离测试树补齐；没有把这些夹具装入正式候选。

**复跑前置条件（2026-09-11 18:00 补记）：** 上表「完整回归」必须显式指定 Studio 测试根，否则会得到 10 项假失败：

```
cd /home/ai/github/1Panel/deploy/h3-mcp
H3_CONNECTOR_TEST_STUDIO_ROOT=<已叠加候选目录或当前线上 release> python3 -m pytest -q tests/
```

不指定时默认根是 studio 源码目录，缺 overlay 安装的 `connector_api.py`/`browser_tasks.py`/`input_view.py`/`multimodal_client.py`，于是 `test_browser_tasks.py` 3 项报 `405 != 403/409`、`test_input_assets.py` 1 项断言失败、`test_multimodal_client_chain.py` 3 项 `ModuleNotFoundError`，合计 `7 failed / 3 errors`。**这些不是发布回归**，已逐项核实。

数字必须连「测试根 + 时间」一起记录：18:02 以根 `releases/20260911-status-fix-r1` 复跑（h3-mcp 侧）为 `499 passed, 1 skipped`；自 17:37 起线上 Studio 实际运行 `releases/20260911-firstframe-four-recipes-r1`（drop-in `90-firstframe-four-recipes.conf`），该根比 status-fix-r1 多 accelerated i2v 等 10 处差异，复跑以其结果为准。

独立审计结论（run 生命周期 3 项已修复、比较横幅时钟偏差本轮修复、上述假失败成因与仍待处理项）见 `audit-20260911-run-lifecycle-and-banner.md`。

## 真实输入和模型阻断

已核对 WorkBuddy 原会话同时含任务编号和原照片文件名。原件 SHA256：
`a80da8df3ab36cb5af5ce8d96792f69a045ae435a41febdc6efec8415232c63a`。
原照片未上传新链路、未绑定新版本，不以聊天截图替代。后续只在明确确认的新版本上执行一次首帧成片，画质交由用户评价。

FL2VA 及编码器/VAE 已在现有 A4 运行环境的权重清单中核对。REF2VA 完整权重此前缺失，正下载至 Ivan 现有外置模型盘；未注册、未加载、不影响现有配方。
下载必须核对 `34038894550` 字节和 SHA256
`9eef934046a0671bc8a5daf87100705e1478419c574cfde70c50fbe6885f76a9`。
不会占用只剩约47GB的 offload 卷放置34GB模型；始终保留至少40GiB磁盘安全线。下载完成不等于参考/Hybrid 通过运行验收。
2026-09-11 06:25（上海时间）下载临时文件为5,930,745,856字节，约17.4%；随后再次核对同一终端句柄仍在运行，未重复启动。此进度是采样值，不代表最终下载或摘要验证完成。

2026-09-11本轮只读连接检查：`ivan-laptop` 在 Tailscale 显示离线，最后在线为上海时间03:30；SSH连接超时。因此没有安装本地组件，也没有执行 Windows 私密配置器实机测试。Ivan GPU主机仍在线，这不是把两台主机混为一谈。

**仍未获得的执行证据：** 当前运行节点需要24fps参考帧；新增兼容性拒绝与解码预算已离线验证，但不代表参考模式已经真实采样。CPU短片测试只证明探测器行为，不代表4K参考视频能进入72GiB内存线，更不代表GPU成片通过。原照片首帧及其他缺证据分支仍需依计划验收，当前不开放这些新配置。

## 不可变候选与切换步骤

当前冻结并复核摘要的候选：Studio `/tmp/h3-multimodal-studio-candidate-r12`（**06:42 构建，不含 12:31 的 run 生命周期修复，已过期，不得再作为当前候选**；线上 Studio 自 17:37 起运行 `releases/20260911-firstframe-four-recipes-r1`），Fleet `/tmp/h3-multimodal-fleet-release-r5`，Control `/tmp/h3-multimodal-control-candidate-r2`，Windows安装包 `/tmp/h3-workbuddy-multimodal-bundle-r2`。这些仅用于离线构建/验收，正式发布必须复制到持久不可变目录并再次校验所有摘要。
Windows包只含 `bridge.cjs`、`configure_bridge.py`、`install_bridge.py`、`SKILL.md`、`multimodal.md` 和摘要清单，不含令牌；服务端上传路由就绪前不能先安装。安装后私密设置入口为 `%USERPROFILE%\.workbuddy\h3-bridge\configure-h3.cmd`，不让模型读取或转发 Token。

1. `scripts/prepare_multimodal_release.py` 从当前 Studio 发布目录叠加独立模块；Control 仅叠加 H3 模块和薄代理，不覆盖脏工作树的 Router 核心。
2. `scripts/freeze_multimodal_fleet.py` 从线上 Fleet、实际后端身份和实际 `--input-directory` 冻结候选。保留四配方历史证据；新增5个已具备权重的专用配置仍无执行资格，另5分支明确显示缺少参考权重。注册不等于放行。
3. 经本次维护窗口确认后，重新核对 Studio/Fleet/三后端真实队列和所有持久任务；一致性备份数据库、项目/资产、两 Control 镜像、Compose 全部覆盖配置、服务定义、私密环境文件和回滚版本。
4. Fleet 保持单路并启用仅指定验收任务的发布门禁。部署必要 Fleet 改动，再同目录切换 Studio 前后端，最后更新两个 Control。Router API 和3060/无关服务不重启。
5. 新 Fleet 环境明确绑定候选 `H3_MULTIMODAL_CATALOG` 与 `H3_RECIPE_POLICY`；不使用旧 `scripts/deploy.py` 归档（其清单不包含本轮新模块）。运行目录和最终配置必须在持久路径，不能以 `/tmp` 候选直接供生产服务。
6. 远端素材路由联调通过后才安装 WorkBuddy 本地组件。验证实际工具发现/认证、原附件上传及端到端摘要、断连回执、六模式准备、页面显示和权限；不因模型缺失静默降级。
7. 指定原照片的首帧任务新版本重新确认后，覆盖按任务准备后端和一次完整成片，核对真实非缓存采样、播放、Range下载、资源释放。未验证的其他执行分支只补必要采样，不重跑四配方比较。
8. 验收通过后，按对应规格/运行版本导入真实证据，再核对 Studio 和两 Control 的环境上限、管理策略以及 Fleet 发布隔离门禁。只开放通过的模式，明确移除遗留的草稿验收阻断，不自动启动旧草稿。

## 回滚与尚未完成

失败先关闭新接单和后台新派发，保留已提交任务对账；不得重提或终止健康任务。任务终结后才恢复旧 Studio/Control/Fleet 配置，旧视频与数据库不删除。Windows 恢复仅限安装回执中且未被用户再次修改的文件。

尚未完成：参考权重下载与校验、维护切换、实际安装后的 WorkBuddy 调用、六模式真实准备及缺证据分支采样、原照片一次成片、正式生成开关最终恢复。无法承诺零风险，也不能用本报告中的离线结果代替这些步骤。

维护窗口授权连续等待尚未获得，最新Tailscale检查仍显示Windows客户端离线；离线候选已就绪，下一步需要用户授权切换并让客户端上线。此状态不是任务完成；不以自动续作消息代替维护授权，不擅自安装、切服务或提交GPU验收任务。
