# H3 MCP 生产发布记录：2026-09-10

## 已上线

用户明确确认维护窗口后，仅切换 Studio 与两个 Control。线上 MCP 地址为
`https://ai-x10drg.taild500c8.ts.net:4001/mcp/h3`。
无凭证请求由原先的 404 改为 401；不是取消认证，也不是 SSE 降级。

- Studio 前后端统一发布目录：`/home/ai/.local/state/h3-studio-ivan-production/releases/20260910-mcp-r1`。
- 两个 Control 镜像：`sha256:1b03dfed91dc698ed473ea450615c4435dcf3ea16224f696a7dfb669789733e1`。
- Studio PID：`2183160`，记录的是验收时采样值。
- 两个 Control 实际 `/health` 均返回 `ok=true,state_store=true`，重启计数均为零。
- 两侧草稿写入启用；两侧 `GENERATION_ENABLED=false`，不能提交新视频。

已核对候选文件哈希和原线上发布基线，没有漂移。保留现有 Compose 全部覆盖、
实际环境及原有挂载，仅追加 H3 MCP 环境与单独内部密钥挂载。
Compose 导出的 `$$` 与容器运行命令的 `$` 是转义差异，已核对，不是启动参数变更。
Control 未定义 Docker healthcheck；使用真实 HTTP 健康检查，首次等待包含原有历史索引初始化。
等待期间没有反复重启已启动实例。

## 真实接口验收

- HTTPS MCP 初始化及九工具发现通过。
- Ivan 上 Python 使用默认证书验证访问同一地址得到 401，确认客户端主机可达且路由存在。
  Windows curl 单独报 `CRYPT_E_REVOCATION_OFFLINE`（吊销服务器不可达）；没有关闭证书校验
  或修改系统设置，也不把该命令行探测当成 WorkBuddy 原生连接验收。
- `h3_capabilities` 返回草稿写入可用、视频生成关闭。
- 保存一个明确标记的验收草稿：`fe9f8098adc3`。
- 操作编号：`deploy-20260910-mcp-draft-001`；重新初始化后按此操作号找回相同任务通过。
- 按任务号查询通过；访问其他账号的历史任务被拒绝。
- 没有调用 `h3_confirm_prompt`，没有调用 `h3_start_preview`，没有替用户审核视频。
- 五个原有项目完整 JSON 哈希保持不变；仅新增上述未审批草稿。
- 切换前后 Fleet 执行数及排队数均为零；其他 Docker 容器 ID、启动时间均未变化。

这些是服务端真实协议验收，不是 WorkBuddy 内自然语言验收、视频播放验收或画质结论。
没有重新运行四配方成片、短测或其他 GPU 推理。

## 专用账号与凭证

专用账号为 `workbuddy-ivan-h3`，`models=[]`、`media_models=["siyuan-video"]`，
不授予文本模型、图片或管理员权限。保留现有所有权与 H3 allowlist 检查。
现有通用账号创建接口强制非空文本模型，因此本次使用一次性、仅创建且带审计的
运维初始化，不改变共享接口或绕过认证：先验证账号的名称、额度及其他字段，再将
待持久化记录的文本模型集合限制为空；没有临时把文本权限写入生产。
沿用账号写入锁，已有不同配置时拒绝覆盖。内存测试覆盖创建、重复创建和冲突拒绝，
以及认证层拒绝空文本授权集合访问任何被测文本模型。

临时验收密钥 `898a0f4e9f6a392b` 已在验收后撤销；此处是密钥 ID，不是密钥内容。
没有将密钥写入聊天、安装包、Git 或 Windows 客户端文件。
尚未发放永久客户端 Token，尚未完成 WorkBuddy 原生私密凭证表单接入。
不要把模型权限改成 `*` 来解决界面编辑限制；当前通用账号表单对空文本集合的编辑
兼容性未验证，密钥创建/撤销及账号读取使用原有接口。

## 备份、恢复与待办

私有备份与发布证据目录：
`/home/ai/.local/state/h3-studio-ivan-production/deployment/mcp-release-20260910-r1`。
该目录含私有运行环境和已撤销的临时验收凭据，不可打包上传或复制到 Git。

保存内容包括：原 Studio unit/环境、切换前和停服后的 SQLite 一致性备份、
原 Control 镜像和所有容器状态、Compose 覆盖副本与解析结果、前后容量、
项目哈希、实际 MCP 验收结果、临时密钥撤销回执及本次运维脚本。
原候选验收报告保持原样；不能把旧的“候选未部署”记录回填成当时已上线。

回滚顺序：先停止 MCP 新写入，保留现有任务查询并核对真实队列；确认无执行任务后，
使用私有目录的 `compose-before.json` 恢复两个 Control 的原镜像配置。
Studio 回滚只移除本次 `40-h3-mcp.conf` drop-in（先保存副本），恢复原发布目录并
重新加载 unit；不恢复数据库覆盖新草稿，不删历史，不重提任务。
任何后续回滚仍需对应维护授权；本次没有执行回滚。

剩余交付项：WorkBuddy 原生认证配置及自然语言调用、人工提示词确认、经授权的
历史视频播放验收。未完成这些之前继续关闭 MCP 生成，不声称客户端已完全可用。
